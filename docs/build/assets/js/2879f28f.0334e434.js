"use strict";(self.webpackChunkdocs=self.webpackChunkdocs||[]).push([["9304"],{65966:function(e,n,t){t.r(n),t.d(n,{frontMatter:()=>l,toc:()=>u,default:()=>p,metadata:()=>r,assets:()=>c,contentTitle:()=>d});var r=JSON.parse('{"id":"schema-first-guide","title":"Schema First Guide","description":"Although MikroORM is primarily a \\"code first\\" ORM, it can also be used in a \\"schema first\\" approach.","source":"@site/versioned_docs/version-6.6/schema-first-guide.md","sourceDirName":".","slug":"/schema-first-guide","permalink":"/docs/6.6/schema-first-guide","draft":false,"unlisted":false,"editUrl":"https://github.com/mikro-orm/mikro-orm/edit/master/docs/versioned_docs/version-6.6/schema-first-guide.md","tags":[],"version":"6.6","lastUpdatedBy":"Page Turner Agent","lastUpdatedAt":1774827906000,"frontMatter":{"title":"Schema First Guide"},"sidebar":"docs","previous":{"title":"Recipes","permalink":"/docs/6.6/recipes"},"next":{"title":"Usage with SQL Drivers","permalink":"/docs/6.6/usage-with-sql"}}'),a=t(74848),i=t(84429),s=t(84109),o=t(93703);let l={title:"Schema First Guide"},d,c={},u=[{value:"&quot;Code first&quot; vs &quot;Schema first&quot;",id:"code-first-vs-schema-first",level:2},{value:"What are we building?",id:"what-are-we-building",level:2},{value:"Project setup",id:"project-setup",level:2},{value:"Install",id:"install",level:3},{value:"ECMAScript Modules",id:"ecmascript-modules",level:3},{value:"Configuring TypeScript",id:"configuring-typescript",level:3},{value:"Configuring the CLI",id:"configuring-the-cli",level:3},{value:"Generating initial entities",id:"generating-initial-entities",level:2},{value:"Manipulating entity file locations and names",id:"manipulating-entity-file-locations-and-names",level:2},{value:"Using the generated entities",id:"using-the-generated-entities",level:2},{value:"\u26F3 Checkpoint 1",id:"-checkpoint-1",level:2},{value:"Making changes to existing tables and columns",id:"making-changes-to-existing-tables-and-columns",level:2},{value:"Renaming existing tables and columns",id:"renaming-existing-tables-and-columns",level:3},{value:"&quot;Expand, then contract&quot; migration strategy",id:"expand-then-contract-migration-strategy",level:3},{value:"Naming strategy considerations",id:"naming-strategy-considerations",level:3},{value:"Adding application level logic to entities",id:"adding-application-level-logic-to-entities",level:2},{value:"Naming strategy vs metadata hooks",id:"naming-strategy-vs-metadata-hooks",level:3},{value:"Adding virtual properties",id:"adding-virtual-properties",level:3},{value:"\u26F3 Checkpoint 2",id:"-checkpoint-2",level:2},{value:"Application refactor",id:"application-refactor",level:2},{value:"Move routes into the modules",id:"move-routes-into-the-modules",level:3},{value:"Making the config env dependent",id:"making-the-config-env-dependent",level:3},{value:"Testing the endpoints",id:"testing-the-endpoints",level:3},{value:"Adding better error handling",id:"adding-better-error-handling",level:3},{value:"Adding repositories",id:"adding-repositories",level:3},{value:"Adding input runtime validation via Zod",id:"adding-input-runtime-validation-via-zod",level:3},{value:"Making backwards compatible changes to the database",id:"making-backwards-compatible-changes-to-the-database",level:2},{value:"Reusing the user authentication check",id:"reusing-the-user-authentication-check",level:3},{value:"Modularizing the configuration",id:"modularizing-the-configuration",level:3},{value:"\u26F3 Checkpoint 3",id:"-checkpoint-3",level:2},{value:"Completing the project",id:"completing-the-project",level:2},{value:"Add the remaining article endpoints",id:"add-the-remaining-article-endpoints",level:3},{value:"Embeddable entities",id:"embeddable-entities",level:3},{value:"Embeddable as a group of columns",id:"embeddable-as-a-group-of-columns",level:4},{value:"Embeddable as a type of JSON column",id:"embeddable-as-a-type-of-json-column",level:4},{value:"Formula properties",id:"formula-properties",level:3},{value:"Using the query builder during entity generation",id:"using-the-query-builder-during-entity-generation",level:2},{value:"Deployment",id:"deployment",level:2},{value:"Running without ts-node",id:"running-without-ts-node",level:3},{value:"\u26F3 Checkpoint 4",id:"-checkpoint-4",level:2}];function h(e){let n={a:"a",admonition:"admonition",blockquote:"blockquote",code:"code",em:"em",h2:"h2",h3:"h3",h4:"h4",li:"li",ol:"ol",p:"p",pre:"pre",ul:"ul",...(0,i.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(n.p,{children:'Although MikroORM is primarily a "code first" ORM, it can also be used in a "schema first" approach.'}),"\n",(0,a.jsx)(n.h2,{id:"code-first-vs-schema-first",children:'"Code first" vs "Schema first"'}),"\n",(0,a.jsx)(n.p,{children:'As the names suggest, in a "code first" approach, you write the entity definitions first, and generate the schema definition out of that code (using the schema generator). As a last step, you execute the migration statements to the database server. In a "schema first" approach, you write the schema definition first (or in the case of migrations, write the migrations first), execute it, and generate the entity definitions out of the database (using the entity generator).'}),"\n",(0,a.jsx)(n.p,{children:"Both approaches have some benefits and drawbacks."}),"\n",(0,a.jsx)(n.p,{children:"Code first:"}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"\u2705 No need to get familiar with all SQL options for defining tables, columns, indexes and foreign keys. (It helps if you are though)"}),"\n",(0,a.jsx)(n.li,{children:"\u2705 Easy to port between different database engines (until you opt into engine-specific features)"}),"\n",(0,a.jsx)(n.li,{children:"\u2705 It is trivial to rename tables and columns, as well as add and remove them... As long as you only do one of those three things per entity per migration."}),"\n",(0,a.jsx)(n.li,{children:"\u274C If you aren\u2019t careful, and do multiple changes to one entity in one go, database migrations can cause data loss. Careful manual review of generated migrations is needed to avoid this."}),"\n",(0,a.jsx)(n.li,{children:'\u274C Performance may be suboptimal, as many database features are "out of sight, out of mind".'}),"\n",(0,a.jsxs)(n.li,{children:["\u274C You may be missing out on possible M",":N"," and 1",":N"," relations that would in turn make your application logic simpler."]}),"\n",(0,a.jsx)(n.li,{children:"\u274C Hard to port to and from a different ORM, as often times, features that are named the same may actually work differently, and conversely, the same features may be named differently."}),"\n"]}),"\n",(0,a.jsx)(n.p,{children:"Schema first:"}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsx)(n.li,{children:"\u2705 No need to get familiar with the ORM's options. (It helps if you are though)"}),"\n",(0,a.jsx)(n.li,{children:"\u2705 Easy to port to and from a different ORM (including different versions of the same ORM), even if that ORM is in another language."}),"\n",(0,a.jsx)(n.li,{children:"\u2705 If you\u2019re comfortable with SQL, it is trivial to add new tables, columns and relations, while keeping the entity definitions fully aware of all possible links, and your data safe."}),"\n",(0,a.jsx)(n.li,{children:"\u274C Renames are a bit more involved, because the regenerated entities aren\u2019t part of the rest of your code."}),"\n",(0,a.jsx)(n.li,{children:"\u274C Sufficiently complex schemas can end up triggering bugs in entity generation, that you then need to patch in some way, before your application can even build."}),"\n",(0,a.jsx)(n.li,{children:"\u274C You may be missing out on goodies from the ORM that make application logic simpler."}),"\n",(0,a.jsx)(n.li,{children:'\u274C Likely harder to port to and from a different database engine, as even relatively "simple" database schemas are likely to end up needing database-specific features, which the entity generator will include where supported by the ORM. If using the database to its full potential, such a migration would be even more challenging.'}),"\n"]}),"\n",(0,a.jsx)(n.h2,{id:"what-are-we-building",children:"What are we building?"}),"\n",(0,a.jsx)(n.p,{children:"In the rest of this guide, we will be building an application after first having made the database schema."}),"\n",(0,a.jsxs)(n.p,{children:["We'll end with the same application that you may have already created by following ",(0,a.jsx)(n.a,{href:"./guide",children:'the "code first" guide'}),", but re-create it from scratch again. Reading that guide beforehand is not strictly required, but we will make several references back to it as a point of comparison."]}),"\n",(0,a.jsxs)(n.p,{children:["To take a peek at the final project we will be building, try cloning the ",(0,a.jsxs)(n.a,{href:"https://github.com/mikro-orm/schema-first-guide",children:[(0,a.jsx)(n.code,{children:"mikro-orm/schema-first-guide"})," GitHub project"]}),"."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"git clone https://github.com/mikro-orm/schema-first-guide.git\n"})}),"\n",(0,a.jsx)(n.p,{children:'We will use MySQL for this project. Other database engines follow the same process. We are also assuming you already have MySQL itself installed locally and can connect to it via the username "root" and no password.'}),"\n",(0,a.jsx)(n.p,{children:"In general, if you're building an application from scratch (as opposed to migrating an existing application), you can use GUI tools (e.g. In the case of MySQL, this includes MySQL Workbench) to make this part of the process easier."}),"\n",(0,a.jsx)(n.p,{children:"Here's the MySQL DDL of our initial application (before later migrations), as dumped by a DB creation tool (in this case, MySQL Workbench Forward Engineering):"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sql",metastring:"title='schema.sql'",children:"-- MySQL Workbench Forward Engineering\n\nSET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;\nSET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;\nSET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';\n\n-- -----------------------------------------------------\n-- Schema blog\n-- -----------------------------------------------------\n\n-- -----------------------------------------------------\n-- Schema blog\n-- -----------------------------------------------------\nCREATE SCHEMA IF NOT EXISTS `blog` DEFAULT CHARACTER SET utf8 ;\nUSE `blog` ;\n\n-- -----------------------------------------------------\n-- Table `user`\n-- -----------------------------------------------------\nCREATE TABLE IF NOT EXISTS `user` (\n  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  `full_name` VARCHAR(255) NOT NULL,\n  `email` VARCHAR(255) NOT NULL,\n  `password` VARCHAR(255) NOT NULL,\n  `bio` TEXT NOT NULL,\n  PRIMARY KEY (`id`))\nENGINE = InnoDB;\n\n\n-- -----------------------------------------------------\n-- Table `article`\n-- -----------------------------------------------------\nCREATE TABLE IF NOT EXISTS `article` (\n  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  `slug` VARCHAR(255) NOT NULL,\n  `title` VARCHAR(255) NOT NULL,\n  `description` VARCHAR(1000) NOT NULL,\n  `text` TEXT NOT NULL,\n  `author` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (`id`),\n  UNIQUE INDEX `slug_UNIQUE` (`slug` ASC) VISIBLE,\n  INDEX `fk_article_user1_idx` (`author` ASC) VISIBLE,\n  CONSTRAINT `fk_article_user1`\n    FOREIGN KEY (`author`)\n    REFERENCES `user` (`id`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n\n\n-- -----------------------------------------------------\n-- Table `comment`\n-- -----------------------------------------------------\nCREATE TABLE IF NOT EXISTS `comment` (\n  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  `text` VARCHAR(1000) NOT NULL,\n  `article` INT UNSIGNED NOT NULL,\n  `author` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (`id`),\n  INDEX `fk_comment_article1_idx` (`article` ASC) VISIBLE,\n  INDEX `fk_comment_user1_idx` (`author` ASC) VISIBLE,\n  CONSTRAINT `fk_comment_article1`\n    FOREIGN KEY (`article`)\n    REFERENCES `article` (`id`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION,\n  CONSTRAINT `fk_comment_user1`\n    FOREIGN KEY (`author`)\n    REFERENCES `user` (`id`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n\n\n-- -----------------------------------------------------\n-- Table `tag`\n-- -----------------------------------------------------\nCREATE TABLE IF NOT EXISTS `tag` (\n  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  `name` VARCHAR(20) NOT NULL,\n  PRIMARY KEY (`id`))\nENGINE = InnoDB;\n\n\n-- -----------------------------------------------------\n-- Table `article_tag`\n-- -----------------------------------------------------\nCREATE TABLE IF NOT EXISTS `article_tag` (\n  `article_id` INT UNSIGNED NOT NULL,\n  `tag_id` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (`article_id`, `tag_id`),\n  INDEX `fk_article_tag_tag1_idx` (`tag_id` ASC) VISIBLE,\n  CONSTRAINT `fk_article_tag_article1`\n    FOREIGN KEY (`article_id`)\n    REFERENCES `article` (`id`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION,\n  CONSTRAINT `fk_article_tag_tag1`\n    FOREIGN KEY (`tag_id`)\n    REFERENCES `tag` (`id`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n\n\nSET SQL_MODE=@OLD_SQL_MODE;\nSET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;\nSET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;\n"})}),"\n",(0,a.jsx)(n.p,{children:"But we can place this in an initial migration file, to make our application work on blank MySQL servers as well."}),"\n",(0,a.jsx)(n.p,{children:"We will use MikroORM's migrator to run our migrations, including the initial one. If you were migrating an existing application to MikroORM, you can instead keep doing the migrations in your existing setup, and regenerate your entities on every migration. Once you fully drop your old application, you can generate an initial migration in MikroORM."}),"\n",(0,a.jsx)(n.h2,{id:"project-setup",children:"Project setup"}),"\n",(0,a.jsx)(n.h3,{id:"install",children:"Install"}),"\n",(0,a.jsxs)(n.p,{children:["We will use a similar setup to ",(0,a.jsx)(n.a,{href:"./guide",children:'the "code first" guide'}),"."]}),"\n",(0,a.jsx)(n.p,{children:"Create a folder and cd into it:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"mkdir blog-api && cd blog-api\n"})}),"\n",(0,a.jsx)(n.p,{children:"Init the project:"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm init\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn init\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm init\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun init\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"Install the following:"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install @mikro-orm/core \\\n            @mikro-orm/mysql \\\n            @mikro-orm/migrations \\\n            fastify\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add @mikro-orm/core \\\n            @mikro-orm/mysql \\\n            @mikro-orm/migrations \\\n            fastify\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add @mikro-orm/core \\\n            @mikro-orm/mysql \\\n            @mikro-orm/migrations \\\n            fastify\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add @mikro-orm/core \\\n            @mikro-orm/mysql \\\n            @mikro-orm/migrations \\\n            fastify\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"and some dev dependencies"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install --save-dev @mikro-orm/cli \\\n                       @mikro-orm/entity-generator \\\n                       typescript \\\n                       ts-node \\\n                       @types/node \\\n                       rimraf \\\n                       vitest\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add --dev @mikro-orm/cli \\\n                       @mikro-orm/entity-generator \\\n                       typescript \\\n                       ts-node \\\n                       @types/node \\\n                       rimraf \\\n                       vitest\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add --save-dev @mikro-orm/cli \\\n                       @mikro-orm/entity-generator \\\n                       typescript \\\n                       ts-node \\\n                       @types/node \\\n                       rimraf \\\n                       vitest\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add --dev @mikro-orm/cli \\\n                       @mikro-orm/entity-generator \\\n                       typescript \\\n                       ts-node \\\n                       @types/node \\\n                       rimraf \\\n                       vitest\n"})})})]}),"\n",(0,a.jsx)(n.h3,{id:"ecmascript-modules",children:"ECMAScript Modules"}),"\n",(0,a.jsxs)(n.p,{children:["Just ",(0,a.jsx)(n.a,{href:"/docs/6.6/guide/first-entity#ecmascript-modules",children:'as in the "code first" guide'}),", we'll be using ECMAScript Modules. Make sure you have"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:"title='package.json'",children:'{\n  "type": "module",\n  ...\n}\n'})}),"\n",(0,a.jsx)(n.p,{children:"in your package.json file."}),"\n",(0,a.jsxs)(n.blockquote,{children:["\n",(0,a.jsxs)(n.p,{children:["Note that we don't ",(0,a.jsx)(n.em,{children:"have to"})," use ECMAScript Modules. MikroORM also supports CommonJS. We are using it for the guides, because we are making a new project, in which we ",(0,a.jsx)(n.em,{children:"can"})," use it, as all of our dependencies are ready for ECMAScript Modules."]}),"\n"]}),"\n",(0,a.jsx)(n.h3,{id:"configuring-typescript",children:"Configuring TypeScript"}),"\n",(0,a.jsxs)(n.p,{children:["We will use almost the same config ",(0,a.jsx)(n.a,{href:"/docs/6.6/guide/first-entity#configuring-typescript",children:'as the "code first" guide one'}),". As mentioned there, already, adjust this config if you know what you\u2019re doing."]}),"\n",(0,a.jsxs)(n.p,{children:["We'll include the ts-node config, and add ",(0,a.jsx)(n.code,{children:"emitDecoratorMetadata"}),", because we'll be using the default metadata provider, which requires that of our TypeScript config."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:"title='tsconfig.json'",children:'{\n  "compilerOptions": {\n    "module": "NodeNext",\n    "moduleResolution": "NodeNext",\n    "target": "ES2022",\n    "strict": true,\n    "outDir": "dist",\n    "declaration": true,\n    "emitDecoratorMetadata": true,\n    "experimentalDecorators": true\n  },\n  "include": [\n    "./src/**/*.ts"\n  ],\n  "ts-node": {\n    "esm": true,\n    "transpileOnly": true\n  }\n}\n'})}),"\n",(0,a.jsx)(n.h3,{id:"configuring-the-cli",children:"Configuring the CLI"}),"\n",(0,a.jsx)(n.p,{children:'Configuring the MikroORM CLI tools is essential for the "schema first" approach.  We need the migrator to run our migrations, as well as the entity generator to create our entities out of the schema state.'}),"\n",(0,a.jsx)(n.p,{children:"Here's a basic config we'll start with (and later extend to take full advantage of the entity generator's features):"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"import { defineConfig } from '@mikro-orm/mysql';\nimport { EntityGenerator } from '@mikro-orm/entity-generator';\nimport { Migrator } from '@mikro-orm/migrations';\n\nexport default defineConfig({\n    multipleStatements: true,\n    extensions: [EntityGenerator, Migrator],\n    discovery: {\n        // we need to disable validation for no entities, due to the entity generation\n        warnWhenNoEntities: false,\n    },\n    entities: ['dist/**/*.entity.js'],\n    entitiesTs: ['src/**/*.entity.ts'],\n    host: 'localhost',\n    user: 'root',\n    password: '',\n    dbName: 'blog',\n    // enable debug mode to log SQL queries and discovery information\n    debug: true,\n    migrations: {\n        path: 'dist/migrations',\n        pathTs: 'src/migrations',\n    },\n    entityGenerator: {\n        save: true,\n        path: 'src/modules',\n        esmImport: true,\n        readOnlyPivotTables: true,\n        outputPurePivotTables: true,\n        bidirectionalRelations: true,\n        customBaseEntityName: 'Base',\n        useCoreBaseEntity: true,\n    },\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"And you can also add to your package.json"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:"title='package.json'",children:'{\n  "mikro-orm": {\n    "useTsNode": true\n  }\n}\n'})}),"\n",(0,a.jsxs)(n.p,{children:["Or alternatively, set the environment variable ",(0,a.jsx)(n.code,{children:"MIKRO_ORM_CLI_USE_TS_NODE"})," to a non-empty value."]}),"\n",(0,a.jsx)(n.p,{children:"To keep the example simple, we're having all of our configuration in a single config file, but you may split your config into a shared config and a tool specific config. In that case, you will want to also supply the correct config file for the correct tool upon running it. You will want to wrap those calls in package.json scripts that do that for you."}),"\n",(0,a.jsx)(n.h2,{id:"generating-initial-entities",children:"Generating initial entities"}),"\n",(0,a.jsx)(n.p,{children:"We'll first generate and run an initial migration to generate entities out of. We'll need to add the \"--blank\" option to make it ok for the migration generator that we don't currently have any entities."}),"\n",(0,a.jsx)(n.p,{children:"Run"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm migration:create --initial --blank\n"})}),"\n",(0,a.jsx)(n.p,{children:"And let's edit it to include the contents of the schema:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='migrations/Migration00000000000000.ts'",children:"import { Migration } from '@mikro-orm/migrations';\n\nexport class Migration00000000000000 extends Migration {\n\n  async up(): Promise<void> {\n    await this.execute(`\nCREATE TABLE IF NOT EXISTS \\`user\\` (\n  \\`id\\` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  \\`created_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  \\`updated_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  \\`full_name\\` VARCHAR(255) NOT NULL,\n  \\`email\\` VARCHAR(255) NOT NULL,\n  \\`password\\` VARCHAR(255) NOT NULL,\n  \\`bio\\` TEXT NOT NULL,\n  PRIMARY KEY (\\`id\\`))\nENGINE = InnoDB;\n\nCREATE TABLE IF NOT EXISTS \\`article\\` (\n  \\`id\\` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  \\`created_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  \\`updated_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  \\`slug\\` VARCHAR(255) NOT NULL,\n  \\`title\\` VARCHAR(255) NOT NULL,\n  \\`description\\` VARCHAR(1000) NOT NULL,\n  \\`text\\` TEXT NOT NULL,\n  \\`author\\` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (\\`id\\`),\n  UNIQUE INDEX \\`slug_UNIQUE\\` (\\`slug\\` ASC) VISIBLE,\n  INDEX \\`fk_article_user1_idx\\` (\\`author\\` ASC) VISIBLE,\n  CONSTRAINT \\`fk_article_user1\\`\n    FOREIGN KEY (\\`author\\`)\n    REFERENCES \\`user\\` (\\`id\\`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n\nCREATE TABLE IF NOT EXISTS \\`comment\\` (\n  \\`id\\` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  \\`created_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  \\`updated_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  \\`text\\` VARCHAR(1000) NOT NULL,\n  \\`article\\` INT UNSIGNED NOT NULL,\n  \\`author\\` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (\\`id\\`),\n  INDEX \\`fk_comment_article1_idx\\` (\\`article\\` ASC) VISIBLE,\n  INDEX \\`fk_comment_user1_idx\\` (\\`author\\` ASC) VISIBLE,\n  CONSTRAINT \\`fk_comment_article1\\`\n    FOREIGN KEY (\\`article\\`)\n    REFERENCES \\`article\\` (\\`id\\`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION,\n  CONSTRAINT \\`fk_comment_user1\\`\n    FOREIGN KEY (\\`author\\`)\n    REFERENCES \\`user\\` (\\`id\\`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n\nCREATE TABLE IF NOT EXISTS \\`tag\\` (\n  \\`id\\` INT UNSIGNED NOT NULL AUTO_INCREMENT,\n  \\`created_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  \\`updated_at\\` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  \\`name\\` VARCHAR(20) NOT NULL,\n  PRIMARY KEY (\\`id\\`))\nENGINE = InnoDB;\n\nCREATE TABLE IF NOT EXISTS \\`article_tag\\` (\n  \\`article_id\\` INT UNSIGNED NOT NULL,\n  \\`tag_id\\` INT UNSIGNED NOT NULL,\n  PRIMARY KEY (\\`article_id\\`, \\`tag_id\\`),\n  INDEX \\`fk_article_tag_tag1_idx\\` (\\`tag_id\\` ASC) VISIBLE,\n  CONSTRAINT \\`fk_article_tag_article1\\`\n    FOREIGN KEY (\\`article_id\\`)\n    REFERENCES \\`article\\` (\\`id\\`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION,\n  CONSTRAINT \\`fk_article_tag_tag1\\`\n    FOREIGN KEY (\\`tag_id\\`)\n    REFERENCES \\`tag\\` (\\`id\\`)\n    ON DELETE NO ACTION\n    ON UPDATE NO ACTION)\nENGINE = InnoDB;\n    `);\n  }\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"Then run the migration with"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm migration:up\n"})}),"\n",(0,a.jsx)(n.p,{children:"And now, you can generate the initial entities with"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm generate-entities --save\n"})}),"\n",(0,a.jsx)(n.p,{children:"If all is good to this point, you should be seeing the following directory structure"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{children:"\u251C\u2500\u2500 package.json\n\u251C\u2500\u2500 src\n\u2502   \u251C\u2500\u2500 mikro-orm.config.ts\n\u2502   \u2514\u2500\u2500 modules\n\u2502       \u251C\u2500\u2500 Article.ts\n\u2502       \u251C\u2500\u2500 ArticleTag.ts\n\u2502       \u251C\u2500\u2500 Base.ts\n\u2502       \u251C\u2500\u2500 Comment.ts\n\u2502       \u251C\u2500\u2500 Tag.ts\n\u2502       \u2514\u2500\u2500 User.ts\n\u2514\u2500\u2500 tsconfig.json\n"})}),"\n",(0,a.jsx)(n.h2,{id:"manipulating-entity-file-locations-and-names",children:"Manipulating entity file locations and names"}),"\n",(0,a.jsxs)(n.p,{children:["You may have noticed that the files aren\u2019t following the ",(0,a.jsx)(n.code,{children:"*.entity.ts"})," suffix we configured initially. Further, they're all under one folder. Both of these are because of the default names the entity generator uses. We can override the ",(0,a.jsx)(n.code,{children:"fileName"})," option in the config to save our files in different locations, and add suffixes:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"import { defineConfig } from '@mikro-orm/mysql';\nimport { EntityGenerator } from '@mikro-orm/entity-generator';\nimport { Migrator } from '@mikro-orm/migrations';\n\nexport default defineConfig({\n  // rest of the config\n  entityGenerator: {\n    fileName: (entityName) => {\n      switch (entityName) {\n        case 'Article':\n        case 'ArticleTag':\n        case 'Tag':\n        case 'Comment':\n          return `article/${entityName.toLowerCase()}.entity`;\n        case 'User':\n          return `user/${entityName.toLowerCase()}.entity`;\n        default:\n          return `common/${entityName.toLowerCase()}.entity`;\n      }\n    },\n    // rest of the entity generator config\n  }\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"If you first remove all files from the modules folder (or just remove the modules folder itself), and then re-run the entity generator, you should now instead see the following directory structure:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{children:"\u251C\u2500\u2500 package.json\n\u251C\u2500\u2500 src\n\u2502   \u251C\u2500\u2500 mikro-orm.config.ts\n\u2502   \u2514\u2500\u2500 modules\n\u2502       \u251C\u2500\u2500 article\n\u2502       \u2502   \u251C\u2500\u2500 article.entity.ts\n\u2502       \u2502   \u251C\u2500\u2500 articletag.entity.ts\n\u2502       \u2502   \u251C\u2500\u2500 tag.entity.ts\n\u2502       \u2502   \u2514\u2500\u2500 comment.entity.ts\n\u2502       \u251C\u2500\u2500 common\n\u2502       \u2502   \u2514\u2500\u2500 base.entity.ts\n\u2502       \u2514\u2500\u2500 user\n\u2502           \u2514\u2500\u2500 user.entity.ts\n\u2514\u2500\u2500 tsconfig.json\n"})}),"\n",(0,a.jsx)(n.p,{children:'When re-generating the entities later, you will want to first remove all files with the suffix ".entity.ts".'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx rimraf -g ./src/modules/**/*.entity.ts\n"})}),"\n",(0,a.jsx)(n.p,{children:"Because we'll be regenerating entities a lot, and doing so requires removal of the old ones first, let's add a script in package.json for that:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:'title="package.json"',children:'  "scripts": {\n    "regen": "rimraf -g ./src/modules/**/*.entity.ts && mikro-orm-esm generate-entities --save"\n  }\n'})}),"\n",(0,a.jsx)(n.p,{children:"And now, you can call"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm run regen\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn regen\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm run regen\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun run regen\n"})})})]}),"\n",(0,a.jsx)(n.h2,{id:"using-the-generated-entities",children:"Using the generated entities"}),"\n",(0,a.jsx)(n.p,{children:"Because the generated entities now match our runtime configuration, we can init the ORM in our application, and they should be picked up."}),"\n",(0,a.jsx)(n.p,{children:'We\'re going to use a similar approach for our application organization as the one in the "code first" guide.'}),"\n",(0,a.jsx)(n.p,{children:"Specifically, our DB wrapper:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/db.ts"',children:'import {\n  type EntityManager,\n  type EntityRepository,\n  MikroORM,\n  type Options,\n} from "@mikro-orm/mysql";\nimport config from "./mikro-orm.config.js";\nimport { Article } from "./modules/article/article.entity.js";\nimport { Tag } from "./modules/article/tag.entity.js";\nimport { User } from "./modules/user/user.entity.js";\nimport { Comment } from "./modules/article/comment.entity.js";\n\nexport interface Services {\n  orm: MikroORM;\n  em: EntityManager;\n  user: EntityRepository<User>;\n  article: EntityRepository<Article>;\n  tag: EntityRepository<Tag>;\n  comment: EntityRepository<Comment>;\n}\n\nlet cache: Services;\n\nexport async function initORM(options?: Options): Promise<Services> {\n  if (cache) {\n    return cache;\n  }\n\n  const orm = await MikroORM.init({\n    ...config,\n    ...options,\n  });\n\n  return (cache = {\n    orm,\n    em: orm.em,\n    user: orm.em.getRepository(User),\n    article: orm.em.getRepository(Article),\n    tag: orm.em.getRepository(Tag),\n    comment: orm.em.getRepository(Comment),\n  });\n}\n'})}),"\n",(0,a.jsx)(n.p,{children:"The app itself:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"import { RequestContext } from '@mikro-orm/core';\nimport { fastify } from 'fastify';\nimport { initORM } from './db.js';\n\nexport async function bootstrap(port = 3001, migrate = true) {\n  const db = await initORM({\n      ensureDatabase: { create: false },\n  });\n\n  if (migrate) {\n    // sync the schema\n    await db.orm.migrator.up();\n  }\n\n  const app = fastify();\n\n  // register request context hook\n  app.addHook('onRequest', (request, reply, done) => {\n    RequestContext.create(db.em, done);\n  });\n\n  // shut down the connection when closing the app\n  app.addHook('onClose', async () => {\n    await db.orm.close();\n  });\n\n  // register routes here\n  app.get('/article', async (request) => {\n    const { limit, offset } = request.query as {\n      limit?: number;\n      offset?: number;\n    };\n    const [items, total] = await db.article.findAndCount(\n      {},\n      {\n        limit,\n        offset,\n      }\n    );\n\n    return { items, total };\n  });\n\n  const url = await app.listen({ port });\n\n  return { app, url };\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"And the server entry point:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/server.ts"',children:"import { bootstrap }  from './app.js';\n\ntry {\n  const { url } = await bootstrap();\n  console.log(`server started at ${url}`);\n} catch (e) {\n  console.error(e);\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"Finally, let's add a script in package.json to start the application, as well as a script to check our code:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:'title="package.json"',children:'{\n  "scripts": {\n    "check": "tsc --noEmit",\n    "start": "node --no-warnings=ExperimentalWarning --loader ts-node/esm src/server.ts"\n  }\n}\n'})}),"\n",(0,a.jsx)(n.p,{children:"While you don\u2019t need to run the check script before starting the application, you may find it convenient to check for errors after significant changes."}),"\n",(0,a.jsx)(n.h2,{id:"-checkpoint-1",children:"\u26F3 Checkpoint 1"}),"\n",(0,a.jsxs)(n.p,{children:["At this point, we have an application similar to the one at ",(0,a.jsx)(n.a,{href:"/docs/6.6/guide/project-setup#-checkpoint-3",children:'"Checkpoint 3" of the "code first" guide'}),". The application itself can only list articles, which don\u2019t exist yet, unless we manually add them with SQL queries. However, we already defined all the entities we'll use. We'll later do some tweaks on top of the generated entities to showcase the full extent of the entity generator's features in a useful way. However, you're already at a point where you can use the generated entities \"as is\" in your application code, and code the remaining logic around them."]}),"\n",(0,a.jsxs)(n.p,{children:["You can verify the application is working ok by starting it, and opening ",(0,a.jsx)(n.a,{href:"https://localhost:3001/article",children:"https://localhost:3001/article"})," in your browser."]}),"\n",(0,a.jsx)(n.h2,{id:"making-changes-to-existing-tables-and-columns",children:"Making changes to existing tables and columns"}),"\n",(0,a.jsx)(n.p,{children:"Given the current simplicity of our application, we don't have to worry about compatibility. We can just run"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm migration:create --blank\n"})}),"\n",(0,a.jsx)(n.p,{children:"to create a new empty migration, prepare whatever SQL statements we need to perform in it, run"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm migration:up\n"})}),"\n",(0,a.jsx)(n.p,{children:"and finally re-generate the entities with"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm run regen\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn regen\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm run regen\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun run regen\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"This flow gets a bit more complex once your application grows enough that the rest of your code actually references individual entities and properties, meaning you can't remove or rename things without considering these usages."}),"\n",(0,a.jsx)(n.h3,{id:"renaming-existing-tables-and-columns",children:"Renaming existing tables and columns"}),"\n",(0,a.jsx)(n.p,{children:"When you would like to rename a table or a column, or even adjust the names of classes and properties, you should do so in code first. Use your IDE to rename all usages. In the case of class names, you should also rename the file and its imports. Once you\u2019ve done so, you can then continue with the rest of the flow as shown above - create a migration in which you do the rename in the database, run it, and regenerate the entities. Try to rebuild your application immediately after entity regeneration. The old files (that you had edited manually) will be removed, but that is ok, because thanks to the migration, the new ones will now have the correct names already working with the rest of your application. It is possible that entity regeneration will reveal some relations which were also renamed, due to being named after the table/column that you renamed. In that event, your application will fail to build. You will want to restore your earlier entities from version control, and rename the affected relations, before regenerating the entities again, and trying to build again."}),"\n",(0,a.jsx)(n.p,{children:'Note that with such database renames, any running application instance will break, since it will be referring to a now non-existent name. When running in production, you will want to avoid renames, and instead use the "expand, then contract" migration strategy.'}),"\n",(0,a.jsx)(n.h3,{id:"expand-then-contract-migration-strategy",children:'"Expand, then contract" migration strategy'}),"\n",(0,a.jsx)(n.p,{children:'The way "Expand, then contract" migration strategy works is that you have to do (in this order) the following:'}),"\n",(0,a.jsxs)(n.ol,{children:["\n",(0,a.jsx)(n.li,{children:"Create the new table/column (as a migration + entity regeneration)"}),"\n",(0,a.jsx)(n.li,{children:"Make the new version of your app write to both the old and new table/column (in the same deploy as with step 1 if and only if you also execute migrations automatically on run; otherwise, ensure migration from step 1 is executed before app run)"}),"\n",(0,a.jsx)(n.li,{children:"Copy over old data from the old table/column into the new table/column (in a second migration that doesn't require entity regeneration or application code changes)"}),"\n",(0,a.jsx)(n.li,{children:"Refactor any reads from the old table/column to use the new table/column instead (ideally after old data is already migrated)."}),"\n",(0,a.jsx)(n.li,{children:"After ensuring any read references to the old table/column are gone, stop writing to the old table/column (deploy changed application code without related migrations)."}),"\n",(0,a.jsx)(n.li,{children:"After ensuring any read and write references to the old table/column are gone, remove the old table/column (as a final migration + entity regeneration)."}),"\n"]}),"\n",(0,a.jsx)(n.p,{children:'Technically, you can also apply this strategy if you are using a "code first" approach, and in fact, you very much should. Failure to follow this strategy in a "code first" approach may lead to accidental data loss (unless you carefully review generated migrations), as well as downtime. Failure to follow this strategy in a "schema first" approach leads to downtime on production, and build errors during development.'}),"\n",(0,a.jsx)(n.h3,{id:"naming-strategy-considerations",children:"Naming strategy considerations"}),"\n",(0,a.jsx)(n.p,{children:"The names of your tables and properties don\u2019t have to match exactly the names of classes and properties in your application code. This is what the entity generator does by default to minimize surprises, but you can override this."}),"\n",(0,a.jsx)(n.p,{children:'Let\'s make it so that our tables use the plural form of words, while the entity class names will be singular. In the end, the application code will not need changes, because it is still referring to the singular word "article".'}),"\n",(0,a.jsxs)(n.p,{children:["First, let's add the package ",(0,a.jsx)(n.a,{href:"https://www.npmjs.com/package/pluralize",children:"pluralize"}),", to do the transformation between singular and plural forms automatically."]}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install --save-dev pluralize @types/pluralize\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add --dev pluralize @types/pluralize\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add --save-dev pluralize @types/pluralize\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add --dev pluralize @types/pluralize\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"Next, let's add a migration to rename our tables:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-sh",children:"npx mikro-orm-esm migration:create --blank\n"})}),"\n",(0,a.jsx)(n.p,{children:"and in it,"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='migrations/Migration00000000000001.ts'",children:"import { Migration } from '@mikro-orm/migrations';\n\nexport class Migration00000000000001 extends Migration {\n  async up(): Promise<void> {\n    await this.execute(`\nRENAME TABLE\n    \\`article\\` TO \\`articles\\`,\n    \\`article_tag\\` TO \\`article_tags\\`,\n    \\`tag\\` TO \\`tags\\`,\n    \\`comment\\` TO \\`comments\\`,\n    \\`user\\` TO \\`users\\`\n    `);\n  }\n  async down(): Promise<void> {\n      await this.execute(`\nRENAME TABLE\n    \\`articles\\` TO \\`article\\`,\n    \\`article_tags\\` TO \\`article_tag\\`,\n    \\`tags\\` TO \\`tag\\`,\n    \\`comments\\` TO \\`comment\\`,\n    \\`users\\` TO \\`user\\`\n    `);\n  }\n}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["If you now just run the migration and regenerate, you will see your entities with plural form. To keep them in singular form, we can override the ",(0,a.jsx)(n.code,{children:"getEntityName"})," method of the UnderscoreNamingStrategy (which is the default naming strategy)."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"import { UnderscoreNamingStrategy } from \"@mikro-orm/core\";\nimport pluralize from 'pluralize';\n// rest of imports\n\nexport default defineConfig({\n  // rest of the config\n  namingStrategy: class extends UnderscoreNamingStrategy {\n    override getEntityName(tableName: string, schemaName?: string): string {\n      return pluralize.singular(super.getEntityName(tableName, schemaName));\n    }\n  },\n  entityGenerator: {\n    // rest of entity generator config\n  }\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"With this addition, if you regenerate the entities now, the classes, and the respective file names will now still be in singular form, as they were before."}),"\n",(0,a.jsxs)(n.p,{children:["You may notice that the ",(0,a.jsx)(n.code,{children:"tableName"})," option is also added to all entities. That is because there is a separate method in the naming strategy - ",(0,a.jsx)(n.code,{children:"classToTableName"})," - about converting class names back to table names. The entity generator checks if this method produces the correct table, and if not, it adds the ",(0,a.jsx)(n.code,{children:"tableName"})," option to ensure the correct table is used in the end. You may override the ",(0,a.jsx)(n.code,{children:"classToTableName"})," method in the naming strategy if you wish to instead convert the singular form to plural automatically, and thus omit the ",(0,a.jsx)(n.code,{children:"tableName"}),' option once again. The entity generator will ensure that any errors made by "pluralize" would be mitigated by an explicit ',(0,a.jsx)(n.code,{children:"tableName"})," entry. Alternatively, you may keep the ",(0,a.jsx)(n.code,{children:"classToTableName"})," method to its default, and keep the ",(0,a.jsx)(n.code,{children:"tableName"})," options around, to make your generated entities code searchable by the table names."]}),"\n",(0,a.jsxs)(n.p,{children:["There's also the ",(0,a.jsx)(n.code,{children:"columnNameToProperty"})," method, which, as the name suggests, tells the entity generator what property name to produce for a given column name. Similarly, there is ",(0,a.jsx)(n.code,{children:"propertyToColumnName"})," that does the reverse. If there is a mismatch between the two, the options ",(0,a.jsx)(n.code,{children:"fieldName"})," or ",(0,a.jsx)(n.code,{children:"fieldNames"})," will be filled with the names of the columns."]}),"\n",(0,a.jsx)(n.h2,{id:"adding-application-level-logic-to-entities",children:"Adding application level logic to entities"}),"\n",(0,a.jsx)(n.p,{children:'While there is a lot you can do on DB schema level with foreign key relations, check constraints, unique indexes and generated columns, there are some things that can\'t be determined by the schema alone. At the same time, in a "schema first" approach, you have to keep your entities able to be regenerated at any time. To bridge the gap between these two seemingly conflicting goals, the entity generator has two callbacks that it calls during the entity generation process. In them, you can manipulate the entity metadata, which will in turn influence the generated code in the end. You should keep your modifications during those hooks as simple as possible, to keep your code as portable as possible.'}),"\n",(0,a.jsxs)(n.p,{children:["The two configuration options are ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," and ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"}),". The first is made immediately after getting the raw metadata from your database, and the second is run after the entity generator goes through all the things it normally infers automatically for you from that metadata. Things like M",":N"," relations, inverse sides of relations, base class and more. You can think of ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," as the place to opt into extra features, and ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"})," as the place to opt out of features that you were otherwise opted into."]}),"\n",(0,a.jsx)(n.p,{children:'If you went through the whole "code first" guide before, and now are going over through this guide, you may have noticed that we are missing a few things in the entity definitions. Let\'s add some of them.'}),"\n",(0,a.jsxs)(n.p,{children:['First, let\'s make the "text" of the "article" be lazy. Also, let\'s make the "password" lazy too, as well as make it "hidden", to avoid accidentally leaking it in responses. We\'ll do that in the ',(0,a.jsx)(n.code,{children:"onInitialMetadata"})," hook, though these changes in particular can be done in ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"})," just the same."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"// rest of imports\n\nexport default defineConfig({\n  // rest of the config\n  entityGenerator: {\n    onInitialMetadata: (metadata, platform) => {\n      const userEntity = metadata.find(meta => meta.className === 'User');\n      if (userEntity) {\n        const passwordProp = userEntity.properties.password;\n        passwordProp.hidden = true;\n        passwordProp.lazy = true;\n      }\n\n      const articleEntity = metadata.find(meta => meta.className === 'Article');\n      if (articleEntity) {\n        const textProp = articleEntity.properties.text;\n        textProp.lazy = true;\n      }\n    },\n    // rest of entity generator config\n  }\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:'When it comes to handling the password hashing and verification, we could register global hooks to handle the password. That would be similar to what the "code first" guide does, except doing that would not be doing it at the entity, but globally. A current limitation of the entity generator is that you can\'t add hooks to the entity itself. However, there is an easy workaround that you may in fact find more convenient and ultimately less magical to work with - custom types. That is, we can define a custom type object for the password, which will let us verify the password and hash it automatically on writes.'}),"\n",(0,a.jsx)(n.p,{children:"Let's add this type, and make the password prop of the User entity use it. We'll use argon2 as the hashing function, so first, install it with"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install argon2\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add argon2\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add argon2\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add argon2\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:'The next step is to create the class that our DB value will be transformed to and from. Let\'s add it to the "users" module. We\'ll use the suffix "runtimeType" to make it clear this will be set as the runtime type at an entity. We\'ll also make the type automatically rehash the password on successful verification if needed.'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/password.runtimeType.ts',children:"import { hash, verify, needsRehash, Options } from 'argon2';\n\nconst hashOptions: Options = {\n  hashLength: 100\n};\n\nexport class Password {\n  static async fromRaw(raw: string): Promise<Password>\n  {\n      return new Password(await hash(raw, hashOptions));\n  }\n\n  static fromHash(hash: string): Password\n  {\n    return new Password(hash);\n  }\n\n  #hash: string;\n\n  private constructor(hash: string) {\n    this.#hash = hash;\n  }\n\n  verify(raw: string): Promise<boolean> {\n    return verify(this.#hash, raw, hashOptions);\n  }\n\n  needsRehash(): boolean\n  {\n    return needsRehash(this.#hash, hashOptions);\n  }\n\n  async verifyAndMaybeRehash(raw: string): Promise<boolean> {\n    const verifyResult = await this.verify(raw);\n    if (verifyResult && this.needsRehash()) {\n      this.#hash = await hash(raw, hashOptions);\n    }\n    return verifyResult;\n  }\n    \n  toString() {\n    return this.#hash;\n  }\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"and then add the ORM custom type that does the transformation:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/password.type.ts"',children:"import { type Platform, type TransformContext, Type } from '@mikro-orm/core';\nimport { Password } from './password.runtimeType.js';\n\nexport class PasswordType extends Type<Password, string> {\n\n  convertToJSValue(value: string, platform: Platform): Password {\n    return Password.fromHash(value);\n  }\n\n  convertToDatabaseValue(value: Password, platform: Platform, context?: TransformContext): string {\n    return `${value}`;\n  }\n\n  compareAsType() {\n    return 'string';\n  }\n\n}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Now, let's modify our ",(0,a.jsx)(n.code,{children:"fileName"})," and ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," functions to recognize these two new files and associate the password with them."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"// rest of imports\n\nexport default defineConfig({\n  // rest of the config\n  entityGenerator: {\n    fileName: (entityName) => {\n      switch (entityName) {\n        case 'Article':\n        case 'ArticleTag':\n        case 'Tag':\n        case 'Comment':\n          return `article/${entityName.toLowerCase()}.entity`;\n        case 'User':\n          return `user/${entityName.toLowerCase()}.entity`;\n        case 'Password':\n          return `user/password.runtimeType`;\n        case 'PasswordType':\n          return `user/password.type`;\n        default:\n          return `common/${entityName.toLowerCase()}.entity`;\n      }\n    },\n    onInitialMetadata: (metadata, platform) => {\n      const userEntity = metadata.find(meta => meta.className === 'User');\n      if (userEntity) {\n        const passwordProp = userEntity.properties.password;\n        passwordProp.hidden = true;\n        passwordProp.lazy = true;\n        passwordProp.type = 'PasswordType';\n        passwordProp.runtimeType = 'Password';\n      }\n\n      const articleEntity = metadata.find(meta => meta.className === 'Article');\n      if (articleEntity) {\n        const textProp = articleEntity.properties.text;\n        textProp.lazy = true;\n      }\n    },\n    // rest of entity generator config\n  }\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"After regeneration, you would be able to do the login in app.ts like so:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"import { RequestContext, EntityData } from '@mikro-orm/core';\nimport { fastify } from 'fastify';\nimport { initORM } from './db.js';\nimport { User } from './modules/user/user.entity.js';\nimport { Password } from './modules/user/password.runtimeType.js';\n\nconst emptyHash = await Password.fromRaw('');\n\n//...\n\n  // register new user\n  app.post('/sign-up', async request => {\n    const body = request.body as EntityData<User, true>;\n\n    if (!body.email || !body.fullName || !body.password) {\n      throw new Error('One of required fields is missing: email, fullName, password');\n    }\n\n    if ((await db.user.count({ email: body.email })) > 0) {\n      throw new Error('This email is already registered, maybe you want to sign in?');\n    }\n\n    const user = db.user.create({\n      fullName: body.fullName,\n      email: body.email,\n      password: await Password.fromRaw(body.password),\n      bio: body.bio ?? '',\n    });\n    await db.em.persist(user).flush();\n\n    // after flush, we have the `user.id` set\n    console.log(`User ${user.id} created`);\n\n    return user;\n  });\n\n  app.post('/sign-in', async request => {\n    const { email, password } = request.body as { email: string; password: string };\n    const err = new Error('Invalid combination of email and password');\n    if (password === '' || email === '') {\n      throw err;\n    }\n\n    const user = await db.user.findOne({ email }, {\n      populate: ['password'], // password is a lazy property, we need to populate it\n    })\n      // On failure, we return a pseudo user with an empty password hash.\n      // This approach minimizes the effectiveness of timing attacks\n      ?? { password: emptyHash };\n\n    if (await user.password.verifyAndMaybeRehash(password)) {\n      await db.em.flush();\n      return user;//password is a hidden property, so it won't be returned, even on success.\n    }\n\n    throw err;\n  });\n"})}),"\n",(0,a.jsx)(n.h3,{id:"naming-strategy-vs-metadata-hooks",children:"Naming strategy vs metadata hooks"}),"\n",(0,a.jsx)(n.p,{children:'It may look like the naming strategy is a more specialized version of the metadata hooks, but there is also one critical difference between changing the name from one vs the other. With a naming strategy, all references are also updated with the new name. With a metadata hook, changing the "original" does not update any references to it. You may update the references yourself, but doing so is less efficient than just overriding the naming strategy.'}),"\n",(0,a.jsx)(n.p,{children:'But efficiency aside, this "loophole" can in fact be beneficial. We can use mapped superclasses. To do that, rename an entity via the application hooks, and then create a class with the original name, to take the place of the original class. The new "manual" class should inherit from the generated class.'}),"\n",(0,a.jsx)(n.p,{children:"This approach can be used to mitigate any shortcoming of the entity generator. Most notably, it is useful to create constructor functions and other helper methods, as the generator doesn\u2019t give you any means to add such."}),"\n",(0,a.jsxs)(n.p,{children:["Let's extend the article in this fashion. First, let's adjust our config. We should use a different suffix from \".entity\" for our custom entity class, so that we don't wipe it upon regeneration. We'll also need to recognize these new suffixes as entities too. Let's use the suffix \".customEntity\". We'll also need to adjust the fileName to give the proper paths, and do the rename of the original \"Article\" entity to something else in ",(0,a.jsx)(n.code,{children:"onInitialMetadata"}),". Let's say we'll make it a convention for this project to prefix such class names with \"_\"."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"// rest of imports\n\nexport default defineConfig({\n  // rest of the config\n  entities: ['dist/**/*.customEntity.js', 'dist/**/*.entity.js'],\n  entitiesTs: ['src/**/*.customEntity.ts', 'src/**/*.entity.ts'],\n  // rest of the config\n  entityGenerator: {\n    fileName: (entityName) => {\n      switch (entityName) {\n        case '_Article':\n          return `article/article.entity`;\n        case 'Article':\n          return `article/article.customEntity`;\n        case 'ArticleTag':\n        case 'Tag':\n        case 'Comment':\n          return `article/${entityName.toLowerCase()}.entity`;\n        case 'User':\n          return `user/${entityName.toLowerCase()}.entity`;\n        case 'Password':\n          return `user/password.runtimeType`;\n        case 'PasswordType':\n          return `user/password.type`;\n        default:\n          return `common/${entityName.toLowerCase()}.entity`;\n      }\n    },\n    onInitialMetadata: (metadata, platform) => {\n      const userEntity = metadata.find(meta => meta.className === 'User');\n      if (userEntity) {\n        const passwordProp = userEntity.properties.password;\n        passwordProp.hidden = true;\n        passwordProp.lazy = true;\n        passwordProp.type = 'PasswordType';\n        passwordProp.runtimeType = 'Password';\n      }\n      \n      const articleEntity = metadata.find(meta => meta.className === 'Article');\n      if (articleEntity) {\n        articleEntity.className = '_Article';\n        articleEntity.abstract = true;\n        const textProp = articleEntity.properties.text;\n        textProp.lazy = true;\n      }\n    },\n    // rest of entity generator config\n  }\n});\n"})}),"\n",(0,a.jsxs)(n.p,{children:['And try to regenerate the entities... Oops, you\'ll crash the entity generator. What happened? The "Article" entity is involved in a M',":N"," relationship, and upon trying to connect it on the Users end, it was not found, which is not OK. This is now a case where we need to bring in ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"}),", so that we only swap our the class after the M",":N"," discovery has already happened."]}),"\n",(0,a.jsx)(n.p,{children:"Change the config to:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"// rest of imports\n\nexport default defineConfig({\n  // rest of the config\n  entities: ['dist/**/*.entity.js', 'dist/**/*.customEntity.js'],\n  entitiesTs: ['src/**/*.entity.ts', 'src/**/*.customEntity.ts'],\n  // rest of the config\n  entityGenerator: {\n    fileName: (entityName) => {\n      switch (entityName) {\n        case '_Article':\n          return `article/article.entity`;\n        case 'Article':\n          return `article/article.customEntity`;\n        case 'ArticleTag':\n        case 'Tag':\n        case 'Comment':\n          return `article/${entityName.toLowerCase()}.entity`;\n        case 'User':\n          return `user/${entityName.toLowerCase()}.entity`;\n        case 'Password':\n          return `user/password.runtimeType`;\n        case 'PasswordType':\n          return `user/password.type`;\n        default:\n          return `common/${entityName.toLowerCase()}.entity`;\n      }\n    },\n    onInitialMetadata: (metadata, platform) => {\n      const userEntity = metadata.find(meta => meta.className === 'User');\n      if (userEntity) {\n        const passwordProp = userEntity.properties.password;\n        passwordProp.hidden = true;\n        passwordProp.lazy = true;\n        passwordProp.type = 'PasswordType';\n        passwordProp.runtimeType = 'Password';\n      }\n      \n      const articleEntity = metadata.find(meta => meta.className === 'Article');\n      if (articleEntity) {\n        const textProp = articleEntity.properties.text;\n        textProp.lazy = true;\n      }\n    },\n    onProcessedMetadata: (metadata, platform) => {\n      const articleEntity = metadata.find(meta => meta.className === 'Article');\n      if (articleEntity) {\n        articleEntity.className = '_Article';\n        articleEntity.abstract = true;\n      }\n    },\n    // rest of entity generator config\n  }\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"Regenerating the entities should now work. However, the code doesn't build for now."}),"\n",(0,a.jsx)(n.p,{children:"To fix this, first, let's add the actual custom entity class. We'll add a slug function as part of the custom constructor."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.customEntity.ts"',children:"import { Entity, type Rel } from '@mikro-orm/core';\nimport { _Article } from './article.entity.js';\nimport { User } from '../user/user.entity.js';\n\nfunction convertToSlug(text: string) {\n  return text\n    .toLowerCase()\n    .replace(/[^\\w ]+/g, '')\n    .replace(/ +/g, '-');\n}\n\n@Entity({ tableName: 'articles' })\nexport class Article extends _Article {\n\n  constructor(title: string, text: string, author: Rel<User>) {\n    super();\n    this.title = title;\n    this.text = text;\n    this.author = author;\n    this.slug = convertToSlug(title);\n    this.description = this.text.substring(0, 999) + '\u2026';\n  }\n\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:'And finally, let\'s edit "db.ts" to reference the proper import. The top should read:'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/db.ts"',children:'import {\n    type EntityManager,\n    type EntityRepository,\n    MikroORM,\n    type Options\n  } from "@mikro-orm/mysql";\n  import config from "./mikro-orm.config.js";\n-  import { Article } from "./modules/article/article.entity.js";\n+  import { Article } from "./modules/article/article.customEntity.js";\n  import { Tag } from "./modules/article/tag.entity.js";\n  import { User } from "./modules/user/user.entity.js";\n  import { Comment } from "./modules/article/comment.entity.js";\n'})}),"\n",(0,a.jsx)(n.p,{children:'However, this approach is one you may want to avoid in a "schema first" flow, because your custom class is now outside the entity generator\'s reach. Renaming the database table requires the extra step of renaming the tableName option in the custom class. Changing any property used in the constructor may break builds. In other words, the custom class requires the same care as the rest of your application code does when it accesses entity classes and properties.'}),"\n",(0,a.jsx)(n.p,{children:"Custom types, like what we did for the password, are also technically outside the entity generator's reach. However, they\u2019re self-contained - they can still exist even if the entity changes shape entirely, and the entity may have a custom type swapped out during a regeneration."}),"\n",(0,a.jsx)(n.p,{children:"Since we did introduce this in our code base though, we should also address another problem this creates. Try to regenerate the entities again. You will notice there's now an error. The error happens because MikroORM is trying to import the \".customEntity\" files, but that file can't run without the generated entity already being present. To fix the problem, we'll need to rename our overrides before regeneration (so that MikroORM doesn't recognize them during entity generation), and restore their names after regeneration."}),"\n",(0,a.jsx)(n.p,{children:"To do this, install renamer:"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install --save-dev renamer\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add --dev renamer\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add --save-dev renamer\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add --dev renamer\n"})})})]}),"\n",(0,a.jsxs)(n.p,{children:["and adjust the ",(0,a.jsx)(n.code,{children:"regen"})," script to:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:'title="package.json"',children:'    "regen": "rimraf -g ./src/**/*.entity.ts && renamer --silent --find /\\\\.customEntity\\\\.ts$/ --replace .customEntity.ts.bak ./src/** && mikro-orm-esm generate-entities --save && renamer --silent --find /\\\\.customEntity\\\\.ts\\\\.bak$/ --replace .customEntity.ts ./src/**",\n'})}),"\n",(0,a.jsx)(n.h3,{id:"adding-virtual-properties",children:"Adding virtual properties"}),"\n",(0,a.jsx)(n.p,{children:"Let's continue re-implementing more of the \"code first\" guide's application. We'll add JWT authentication to our endpoints, in a similar fashion to the way the \"code first\" guide does it - via a virtual property that holds the user's JWT."}),"\n",(0,a.jsxs)(n.p,{children:["First, let's add the property. Inside ",(0,a.jsx)(n.code,{children:"onInitialMetadata"}),", for the user entity, we need to call the ",(0,a.jsx)(n.code,{children:"addProperty()"}),' method with an object representing the new property. The entity generator is optimized to work with objects that are pre-filled with the entire database metadata, and does very few checks for custom properties. So to ensure the generator doesn\'t crash, we should include the same type of information as if this was a real column, but with the added "persist" option set to "false". In our case, a nullable ',(0,a.jsx)(n.code,{children:"varchar(255)"})," column mapped to a regular string type is what we need."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"      const userEntity = metadata.find(meta => meta.className === 'User');\n      if (userEntity) {\n        userEntity.addProperty({\n          persist: false,\n          name: 'token',\n          nullable: true,\n          default: null,\n          defaultRaw: 'null',\n          fieldNames: [platform.getConfig().getNamingStrategy().propertyToColumnName('token')],\n          columnTypes: ['varchar(255)'],\n          type: 'string',\n          runtimeType: 'string',\n        });\n        const passwordProp = userEntity.properties.password;\n        passwordProp.hidden = true;\n        passwordProp.lazy = true;\n        passwordProp.type = 'PasswordType';\n        passwordProp.runtimeType = 'Password';\n      }\n"})}),"\n",(0,a.jsx)(n.p,{children:'Regenerating the entities now will add this property in the entity. You don\'t even need to perform a migration here, since there is no "real" database change involved. From here, we still need to do the same things we had to do in the "code first" guide.'}),"\n",(0,a.jsx)(n.p,{children:"Install fastify JWT:"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install @fastify/jwt\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add @fastify/jwt\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add @fastify/jwt\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add @fastify/jwt\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"Then register it at the top of app.ts, and add jwt verify request hook after the ORM hook (to enable JWT verification to use the DB):"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"import fastifyJWT from '@fastify/jwt';\n\n// ...\n\nconst app = fastify();\n\n// register JWT plugin\napp.register(fastifyJWT, {\n  secret: process.env.JWT_SECRET ?? '12345678', // fallback for testing\n});\n\n// register request context hook\napp.addHook('onRequest', (request, reply, done) => {\n    RequestContext.create(db.em, done);\n});\n\n// register auth hook after the ORM one to use the context\napp.addHook('onRequest', async (request) => {\n  try {\n    const ret = await request.jwtVerify<{ id: number }>();\n    request.user = await db.user.findOneOrFail(ret.id);\n  } catch (e) {\n    app.log.error(e);\n    // ignore token errors, we validate the request.user exists only where needed\n  }\n});\n\n// ...\n"})}),"\n",(0,a.jsx)(n.p,{children:"And also add JWT signing to the login and register endpoints, to enable the client to see the signed JWT:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"// ...\n  app.post('/sign-up', async request => {\n    // ...\n    await db.em.persist(user).flush();\n  \n    // after flush, we have the `user.id` set\n    console.log(`User ${user.id} created`);\n    \n    user.token = app.jwt.sign({ id: user.id });\n  \n    return user;\n  });\n\n  app.post('/sign-in', async request => {\n    // ...\n    const user = await db.user.findOne({ email }, {\n        populate: ['password'], // password is a lazy property, we need to populate it\n      })\n      // On failure, we return a pseudo user with an empty password hash.\n      // This approach minimizes the effectiveness of timing attacks\n      ?? { password: emptyHash, id: 0, token: undefined };\n\n    if (await user.password.verifyAndMaybeRehash(password)) {\n      await db.em.flush();\n      user.token = app.jwt.sign({ id: user.id });\n      return user;\n    }\n\n    throw err;\n  });\n"})}),"\n",(0,a.jsx)(n.p,{children:'And let\'s also now add a "/profile" endpoint, to show us the user currently logged in:'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"app.get('/profile', async request => {\n  if (!request.user) {\n    throw new Error('Please provide your token via Authorization header');\n  }\n\n  return request.user as User;\n});\n"})}),"\n",(0,a.jsx)(n.h2,{id:"-checkpoint-2",children:"\u26F3 Checkpoint 2"}),"\n",(0,a.jsx)(n.p,{children:"Our application now has JWT authentication and profile view. Meanwhile, we also did a full DB change cycle. Before we move onto more entity generation features, let's do some refactoring to make the big app.ts file more manageable, and add some tests. This will make the final version of our application, complete with its additional features, easier to reason about."}),"\n",(0,a.jsx)(n.p,{children:'If you wanted to "manually" verify the application at this stage, you would need to issue the POST requests using curl, Postman or other similar tools. Or alternatively, use fetch() from a browser console or a separate Node REPL.'}),"\n",(0,a.jsx)(n.p,{children:"Like to register:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-js",children:"await fetch(new Request('/sign-up', {\n  method: 'POST',\n  headers: {\n    'Content-Type': 'application/json; charset=utf-8'\n  },\n  body: JSON.stringify({\n    fullName: 'test',\n    email: 'test@example.com',\n    password: '1234'\n  })\n}));\n"})}),"\n",(0,a.jsx)(n.p,{children:"and then to login:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-js",children:"await fetch(new Request('/sign-in', {\n  method: 'POST',\n  headers: {\n    'Content-Type': 'application/json; charset=utf-8'\n  },\n  body: JSON.stringify({\n    email: 'test@example.com',\n    password: '1234'\n  })\n}));\n"})}),"\n",(0,a.jsx)(n.h2,{id:"application-refactor",children:"Application refactor"}),"\n",(0,a.jsx)(n.h3,{id:"move-routes-into-the-modules",children:"Move routes into the modules"}),"\n",(0,a.jsx)(n.p,{children:"Let's first move the routes in app.ts into the appropriate module folders, and connect them back with app.ts."}),"\n",(0,a.jsxs)(n.p,{children:["For each ",(0,a.jsx)(n.code,{children:"*.routes.ts"})," file, we'll export a fastify async plugin, and register our routes. Each route file will be imported with a prefix, to allow them to define whatever routes they like, without conflicting with other ",(0,a.jsx)(n.code,{children:"*.routes.ts"})," files."]}),"\n",(0,a.jsxs)(n.p,{children:["Our basic boilerplate for ",(0,a.jsx)(n.code,{children:"*.routes.ts"})," files:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",children:"import { type FastifyPluginAsync } from 'fastify';\nimport { type Services } from '../../db.js';\n\nexport default (async (app, { db }) => {\n  //routes here\n}) as FastifyPluginAsync<{ db: Services }>;\n"})}),"\n",(0,a.jsx)(n.p,{children:"And specifically:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/user.routes.ts"',children:"import { type FastifyPluginAsync } from 'fastify';\nimport { type Services } from '../../db.js';\nimport { User } from './user.entity.js';\nimport { Password } from './password.runtimeType.js';\nimport { type EntityData } from '@mikro-orm/mysql';\n\nconst emptyHash = await Password.fromRaw('');\n\nexport default (async (app, { db }) => {\n\n  // register new user\n  app.post('/sign-up', async request => {\n    const body = request.body as EntityData<User, true>;\n\n    if (!body.email || !body.fullName || !body.password) {\n      throw new Error('One of required fields is missing: email, fullName, password');\n    }\n\n    if ((await db.user.count({ email: body.email })) > 0) {\n      throw new Error('This email is already registered, maybe you want to sign in?');\n    }\n\n    const user = db.user.create({\n      fullName: body.fullName,\n      email: body.email,\n      password: await Password.fromRaw(body.password),\n      bio: body.bio ?? '',\n    });\n    await db.em.persist(user).flush();\n\n    // after flush, we have the `user.id` set\n    console.log(`User ${user.id} created`);\n\n    user.token = app.jwt.sign({ id: user.id });\n\n    return user;\n  });\n\n  app.post('/sign-in', async request => {\n    const { email, password } = request.body as { email: string; password: string };\n    const err = new Error('Invalid combination of email and password');\n    if (password === '' || email === '') {\n      throw err;\n    }\n\n    const user = await db.user.findOne({ email }, {\n        populate: ['password'], // password is a lazy property, we need to populate it\n      })\n      // On failure, we return a pseudo user with an empty password hash.\n      // This approach minimizes the effectiveness of timing attacks\n      ?? { password: emptyHash, id: 0, token: undefined };\n\n    if (await user.password.verifyAndMaybeRehash(password)) {\n      await db.em.flush();\n      user.token = app.jwt.sign({ id: user.id });\n      return user;//password is a hidden property, so it won't be returned, even on success.\n    }\n\n    throw err;\n  });\n\n  app.get('/profile', async request => {\n    if (!request.user) {\n      throw new Error('Please provide your token via Authorization header');\n    }\n\n    return request.user as User;\n  });\n}) as FastifyPluginAsync<{ db: Services }>;\n"})}),"\n",(0,a.jsx)(n.p,{children:"and also"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.routes.ts"',children:"import { type FastifyPluginAsync } from 'fastify';\nimport { type Services } from '../../db.js';\n\nexport default (async (app, { db }) => {\n  app.get('/', async (request) => {\n    const { limit, offset } = request.query as {\n      limit?: number;\n      offset?: number;\n    };\n    const [items, total] = await db.article.findAndCount(\n      {},\n      {\n        limit,\n        offset,\n      }\n    );\n\n    return { items, total };\n  });\n}) as FastifyPluginAsync<{ db: Services }>;\n"})}),"\n",(0,a.jsxs)(n.p,{children:["and let's also move out the hooks too. These would require we wrap them with ",(0,a.jsx)(n.code,{children:"fastify-plugin"})," instead, since we want these hooks across all prefixes."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/hooks.ts"',children:"import { fastifyPlugin } from 'fastify-plugin';\nimport { type Services } from '../../db.js';\nimport { RequestContext } from '@mikro-orm/mysql';\n\nexport default fastifyPlugin<{db: Services}>(async (app, { db }) => {\n\n  // register request context hook\n  app.addHook('onRequest', (request, reply, done) => {\n    RequestContext.create(db.em, done);\n  });\n\n  // register auth hook after the ORM one to use the context\n  app.addHook('onRequest', async (request) => {\n    try {\n      const ret = await request.jwtVerify<{ id: number }>();\n      request.user = await db.user.findOneOrFail(ret.id);\n    } catch (e) {\n      app.log.error(e);\n      // ignore token errors, we validate the request.user exists only where needed\n    }\n  });\n\n  // shut down the connection when closing the app\n  app.addHook('onClose', async () => {\n    await db.orm.close();\n  });\n\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"Which leaves our app.ts like:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"import { fastify } from 'fastify';\nimport fastifyJWT from '@fastify/jwt';\nimport { initORM } from './db.js';\nimport hooks from './modules/common/hooks.js';\nimport userRoutes from './modules/user/user.routes.js';\nimport articleRoutes from './modules/article/article.routes.js';\n\nexport async function bootstrap(port = 3001, migrate = true) {\n  const db = await initORM({\n      ensureDatabase: { create: false },\n  });\n\n  if (migrate) {\n    // sync the schema\n    await db.orm.migrator.up();\n  }\n\n  const app = fastify();\n\n  // register JWT plugin\n  app.register(fastifyJWT, {\n    secret: process.env.JWT_SECRET ?? '12345678', // fallback for testing\n  });\n\n  await app.register(hooks, { db });\n\n  // register routes here\n  app.register(articleRoutes, { db, prefix: 'article' });\n  app.register(userRoutes, { db, prefix: 'user' });\n\n  const url = await app.listen({ port });\n\n  return { app, url };\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:'which is much nicer. Our URL endpoints are now "/article", "/user/sign-up", "/user/sign-in", "/user/profile".'}),"\n",(0,a.jsx)(n.h3,{id:"making-the-config-env-dependent",children:"Making the config env dependent"}),"\n",(0,a.jsx)(n.p,{children:'We mentioned earlier that you could split your config files if you need tool-specific configs. However, more generally, you will at least want a dev vs prod config, with "dev" basically being "when running the MikroORM CLI", while "prod" would basically be "when the application is running".'}),"\n",(0,a.jsx)(n.p,{children:"We can detect whether we're running in the MikroORM CLI based on the arguments, and act accordingly."}),"\n",(0,a.jsxs)(n.p,{children:["And although we don't ",(0,a.jsx)(n.em,{children:"require"})," a tool-specific config, there is one annoying thing about entity generation that we can tackle with a config adjustment specifically to the entity generator. Because of the renames that we have to do for our entity regeneration after our mapped superclass was introduced, you may have seen your IDE fail to recognize the mapped superclass. And it stays like that until you restart your IDE's typescript server, or cut and paste the mapped superclass reference to force a re-check. We can avoid this annoyance by adjusting our config to not feature the entities at all, but only when running the regenerate-entities command from the MikroORM CLI."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"import {\n    defineConfig,\n    type MikroORMOptions,\n} from '@mikro-orm/mysql';\nimport { UnderscoreNamingStrategy } from '@mikro-orm/core';\nimport { Migrator } from '@mikro-orm/migrations';\nimport pluralize from 'pluralize';\nimport { join } from 'node:path';\n\nconst isInMikroOrmCli = process.argv[1]?.endsWith(join('@mikro-orm', 'cli', 'esm')) ?? false;\nconst isRunningGenerateEntities = isInMikroOrmCli && process.argv[2] === 'generate-entities';\n\nconst mikroOrmExtensions: MikroORMOptions['extensions'] = [Migrator];\nif (isInMikroOrmCli) {\n    mikroOrmExtensions.push((await import('@mikro-orm/entity-generator')).EntityGenerator);\n}\n\nexport default defineConfig({\n  extensions: mikroOrmExtensions,\n  multipleStatements: isInMikroOrmCli,\n  discovery: {\n    warnWhenNoEntities: !isInMikroOrmCli,\n  },\n  entities: isRunningGenerateEntities ? [] : ['dist/**/*.customEntity.js', 'dist/**/*.entity.js'],\n  entitiesTs: isRunningGenerateEntities ? [] : ['src/**/*.customEntity.ts', 'src/**/*.entity.ts'],\n  // rest of the config\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"And with that in place, we can revert the changes we made before to the entity generation process, i.e."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="package.json"',children:'-  "regen": "rimraf -g ./src/**/*.entity.ts && renamer --silent --find /\\\\.customEntity\\\\.ts$/ --replace .customEntity.ts.bak ./src/** && mikro-orm-esm generate-entities --save && renamer --silent --find /\\\\.customEntity\\\\.ts\\\\.bak$/ --replace .customEntity.ts ./src/**",\n+  "regen": "rimraf -g ./src/**/*.entity.ts && mikro-orm-esm generate-entities --save",\n'})}),"\n",(0,a.jsx)(n.p,{children:"and"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm uninstall renamer\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn remove renamer\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm remove renamer\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun remove renamer\n"})})})]}),"\n",(0,a.jsxs)(n.p,{children:["We should further make it so that migrations run in a separate connection where ",(0,a.jsx)(n.code,{children:"multipleStatements"})," is enabled, while it is disabled for everything else, for the sake of security."]}),"\n",(0,a.jsx)(n.p,{children:"Let's make app.ts be like:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/app.ts"',children:"import { fastify } from 'fastify';\nimport fastifyJWT from '@fastify/jwt';\nimport { initORM } from './db.js';\nimport hooks from './modules/common/hooks.js';\nimport userRoutes from './modules/user/user.routes.js';\nimport articleRoutes from './modules/article/article.routes.js';\n\nexport async function bootstrap(port = 3001, migrate = true) {\n  const db = await initORM(migrate ? { multipleStatements: true, ensureDatabase: { create: false } } : {});\n\n  if (migrate) {\n    // sync the schema\n    await db.orm.migrator.up();\n    await db.orm.reconnect({ multipleStatements: false });\n  }\n\n  const app = fastify();\n\n  // register JWT plugin\n  await app.register(fastifyJWT, {\n    secret: process.env.JWT_SECRET ?? '12345678', // fallback for testing\n  });\n\n  await app.register(hooks, { db });\n\n  // register routes here\n  app.register(articleRoutes, { db, prefix: 'article' });\n  app.register(userRoutes, { db, prefix: 'user' });\n\n  const url = await app.listen({ port });\n\n  return { app, url };\n}\n"})}),"\n",(0,a.jsx)(n.h3,{id:"testing-the-endpoints",children:"Testing the endpoints"}),"\n",(0,a.jsx)(n.p,{children:"So far, when we've checked the resulting app, we've been doing so \"manually\". Let's add some tests, so that we can repeatedly check that everything is working as we make further changes and additions."}),"\n",(0,a.jsxs)(n.p,{children:['In a "code first" approach, you can let the schema generator create the test database\'s schema for you, based on your entity definitions. While you ',(0,a.jsx)(n.em,{children:"could"}),' do the same in a "schema first" approach, if your database schema is sufficiently complex, you may end up in a situation where the schema generator will produce something slightly different from your true schema (which may be because of bugs originating in the entity generator not producing the correct/complete code, or because your schema includes features that MikroORM does not track yet in general, like triggers and routines), which will in turn make your test results be off, particularly when said differences are being relied on by your application. The best way to avoid issues like this is to simply run your migrations at the start of the test suite. If you have too many migrations, you may consider occasionally doing a database DDL dump using a tool native to your database engine (e.g. "mysqldump" in the case of MySQL) + a data dump of the MikroORM migrations table. Then execute these before running the rest of the migrations that were created after that dump.']}),"\n",(0,a.jsx)(n.p,{children:"To keep this guide simple, we will just run the migrations."}),"\n",(0,a.jsx)(n.p,{children:"Let's create a test util to init our test database:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="test/utils.ts"',children:"import { bootstrap } from '../src/app.js';\nimport { initORM } from '../src/db.js';\n\nexport async function initTestApp(port: number) {\n  // this will create all the ORM services and cache them\n  await initORM({\n    // no need for debug information, it would only pollute the logs\n    debug: false,\n    // we will use a dynamic name, based on port. This way we can easily parallelize our tests\n    dbName: `blog_test_${port}`,\n    // create the schema so we can use the database\n    ensureDatabase: { create: false },\n    // required for the migrations\n    multipleStatements: true,\n  });\n\n  const { app } = await bootstrap(port);\n\n  return app;\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:'and add a test for our "/article" endpoint:'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="test/article.test.ts"',children:"import { afterAll, beforeAll, expect, test } from 'vitest';\nimport { FastifyInstance } from 'fastify';\nimport { initTestApp } from './utils.js';\n\nlet app: FastifyInstance;\n\nbeforeAll(async () => {\n  // we use different ports to allow parallel testing\n  app = await initTestApp(30001);\n});\n\nafterAll(async () => {\n  // we close only the fastify app - it will close the database connection via onClose hook automatically\n  await app.close();\n});\n\ntest('list all articles', async () => {\n  // mimic the http request via `app.inject()`\n  const res = await app.inject({\n    method: 'get',\n    url: '/article',\n  });\n\n  // assert it was successful response\n  expect(res.statusCode).toBe(200);\n\n  // with expected shape\n  expect(res.json()).toMatchObject({\n    items: [],\n    total: 0,\n  });\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:'If you\'ve previously gone through the "code first" guide, you know this breaks with the error message like'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{children:'FAIL  test/article.test.ts [ test/article.test.ts ]\nTypeError: Unknown file extension ".ts" for /blog-api/src/modules/article/article.entity.ts\n'})}),"\n",(0,a.jsx)(n.p,{children:"and to fix it, we need to adjust the config to add a dynamic import:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="test/utils.ts"',children:"import { bootstrap } from '../src/app.js';\nimport { initORM } from '../src/db.js';\n\nexport async function initTestApp(port: number) {\n  // this will create all the ORM services and cache them\n  await initORM({\n    // no need for debug information, it would only pollute the logs\n    debug: false,\n    // we will use a dynamic name, based on port. This way we can easily parallelize our tests\n    dbName: `blog_test_${port}`,\n    // create the schema so we can use the database\n    ensureDatabase: { create: false },\n    // required for the migrations\n    multipleStatements: true,\n+    // required for vitest\n+    dynamicImportProvider: id => import(id),\n  });\n\n  const { app } = await bootstrap(port);\n\n  return app;\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"And now, trying to run it again... you should see a different error:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{children:"Error: Please provide either 'type' or 'entity' attribute in User.id. If you are using decorators, ensure you have 'emitDecoratorMetadata' enabled in your tsconfig.json.\n"})}),"\n",(0,a.jsxs)(n.p,{children:["But we did add ",(0,a.jsx)(n.code,{children:"emitDecoratorMetadata"})," in our ",(0,a.jsx)(n.code,{children:"tsconfig.json"}),", right? Yes, but vitest uses ESBuild to transpile the sources, and ESBuild doesn\u2019t support this out of the box. There are several solutions to this problem. We may either"]}),"\n",(0,a.jsxs)(n.ol,{children:["\n",(0,a.jsxs)(n.li,{children:["Use ",(0,a.jsx)(n.code,{children:"@mikro-orm/reflection"})," to analyze the sources in a different fashion that doesn't rely on ",(0,a.jsx)(n.code,{children:"emitDecoratorMetadata"}),"."]}),"\n",(0,a.jsxs)(n.li,{children:["Swap out ESBuild for SWC, and ",(0,a.jsx)(n.a,{href:"/docs/6.6/usage-with-transpilers#swc",children:"configure SWC to support decorators"}),"."]}),"\n",(0,a.jsxs)(n.li,{children:["Install ",(0,a.jsx)(n.code,{children:"@anatine/esbuild-decorators"})," and add it to the vitest config."]}),"\n",(0,a.jsx)(n.li,{children:'Adjust the entity generator to always output the "type" property, thus bypassing the need to infer the type in the first place.'}),"\n"]}),"\n",(0,a.jsxs)(n.p,{children:['Option 1 is what the "code first" guide does, and that is a great solution if you are writing the entity definitions manually. Options 2 and 3 are a different alternative you may go for if you need ',(0,a.jsx)(n.code,{children:"emitDecoratorMetadata"})," for other purposes as well. For this guide, we'll go with option 4, because it is the easiest to do."]}),"\n",(0,a.jsx)(n.p,{children:"Add to your config"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:"title='src/mikro-orm.config.ts'",children:"  entityGenerator: {\n    scalarTypeInDecorator: true,\n    // rest of entity generator config\n  }\n"})}),"\n",(0,a.jsxs)(n.p,{children:["and regenerate the entities. You can now run the test without an error. You may also remove ",(0,a.jsx)(n.code,{children:"emitDecoratorMetadata"})," from ",(0,a.jsx)(n.code,{children:"tsconfig.json"})," at this point, unless you need it for another library."]}),"\n",(0,a.jsx)(n.p,{children:"Now that we have the article test working, let's also add tests for the user endpoint. We'll register a user, try to log in with them, see their profile, and remove the user at the end, to keep the test repeatable."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="test/user.test.ts"',children:"import { FastifyInstance } from 'fastify';\nimport { afterAll, beforeAll, expect, test } from 'vitest';\nimport { initTestApp } from './utils.js';\nimport { EntityData } from '@mikro-orm/core';\nimport { User } from '../src/modules/user/user.entity.js';\nimport { initORM } from '../src/db.js';\n\nlet app: FastifyInstance;\n\nbeforeAll(async () => {\n  // we use different ports to allow parallel testing\n  app = await initTestApp(30002);\n});\n\nafterAll(async () => {\n  const db = await initORM();\n  try {\n    const fork = db.em.fork();\n    await fork.removeAndFlush(await fork.findOneOrFail(User, { email: 'foo@bar.com' }));\n  } catch (e: unknown) {\n    console.error(e);\n  }\n  // we close only the fastify app - it will close the database connection via onClose hook automatically\n  await app.close();\n});\n\ntest('full flow', async () => {\n  const res1 = await app.inject({\n    method: 'post',\n    url: '/user/sign-up',\n    payload: {\n      fullName: 'Foo Bar',\n      email: 'foo@bar.com',\n      password: 'password123',\n    },\n  });\n\n  expect(res1.statusCode).toBe(200);\n  expect(res1.json()).toMatchObject({\n    fullName: 'Foo Bar',\n  });\n\n  const res1dup = await app.inject({\n      method: 'post',\n      url: '/user/sign-up',\n      payload: {\n          fullName: 'Foo Bar',\n          email: 'foo@bar.com',\n          password: 'password123',\n      },\n  });\n\n  expect(res1dup.statusCode).toBe(500);\n  expect(res1dup.json()).toMatchObject({\n    message: 'This email is already registered, maybe you want to sign in?',\n  });\n\n  const res2 = await app.inject({\n    method: 'post',\n    url: '/user/sign-in',\n    payload: {\n      email: 'foo@bar.com',\n      password: 'password123',\n    },\n  });\n\n  expect(res2.statusCode).toBe(200);\n  expect(res2.json()).toMatchObject({\n    fullName: 'Foo Bar',\n  });\n\n  const res3 = await app.inject({\n    method: 'post',\n    url: '/user/sign-in',\n    payload: {\n      email: 'foo@bar.com',\n      password: 'password456',\n    },\n  });\n\n  expect(res3.statusCode).toBe(500);\n  expect(res3.json()).toMatchObject({ message: 'Invalid combination of email and password' });\n\n  const res4 = await app.inject({\n    method: 'get',\n    url: '/user/profile',\n    headers: {\n        'Authorization': `Bearer ${res2.json().token}`\n    }\n  });\n  expect(res4.statusCode).toBe(200);\n  expect(res2.json()).toMatchObject(res4.json());\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:"This test should also pass with no errors. If all is good, we can move on to a few more application refactorings."}),"\n",(0,a.jsx)(n.h3,{id:"adding-better-error-handling",children:"Adding better error handling"}),"\n",(0,a.jsx)(n.p,{children:"Let's adjust the application so that it returns appropriate status codes, rather than just show status code 500 on any error. Add a dedicated error class file. As our own convention, let's say we'll be placing custom error classes in files with \".error.ts\" suffix. There is no technical reason for this. It's purely organizational."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/auth.error.ts"',children:"export class AuthError extends Error {}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["And then let's make it so that we return status 401 for this error. Add this handler to ",(0,a.jsx)(n.code,{children:"hooks.ts"}),":"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/hooks.ts"',children:"import { fastifyPlugin } from 'fastify-plugin';\nimport { type Services } from '../../db.js';\nimport { NotFoundError, RequestContext } from '@mikro-orm/mysql';\nimport { AuthError } from './auth.error.js';\n\nexport default fastifyPlugin<{db: Services}>(async (app, { db }) => {\n\n  // rest of the code\n\n  // register global error handler to process 404 errors from `findOneOrFail` calls\n  app.setErrorHandler((error, request, reply) => {\n    if (error instanceof AuthError) {\n      return reply.status(401).send(error);\n    }\n\n    // we also handle not found errors automatically\n    // `NotFoundError` is an error thrown by the ORM via `em.findOneOrFail()` method\n    if (error instanceof NotFoundError) {\n      return reply.status(404).send(error);\n    }\n\n    app.log.error(error);\n    reply.status(500).send(error);\n  }); \n});\n"})}),"\n",(0,a.jsxs)(n.p,{children:["And finally, let's actually throw that error on auth failures. Modify ",(0,a.jsx)(n.code,{children:"user.routes.ts"}),":"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user.routes.ts"',children:"...\nimport { type EntityData } from '@mikro-orm/mysql';\n+ import { AuthError } from '../common/auth.error.js';\n...\n    app.post('/sign-in', async request => {\n      const { email, password } = request.body as { email: string; password: string };\n-      const err = new Error('Invalid combination of email and password');\n+      const err = new AuthError('Invalid combination of email and password');\n...\n    app.get('/profile', async request => {\n      if (!request.user) {\n-          throw new Error('Please provide your token via Authorization header');\n+          throw new AuthError('Please provide your token via Authorization header');\n      }\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"If you try to re-run the tests now, you should see a test failure at the status code check. Let's modify the test too, to reflect our new intended behavior:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="test/user.test.ts"',children:"-  expect(res3.statusCode).toBe(500);\n+  expect(res3.statusCode).toBe(401);\n"})}),"\n",(0,a.jsx)(n.p,{children:"And now, the test passes again."}),"\n",(0,a.jsx)(n.h3,{id:"adding-repositories",children:"Adding repositories"}),"\n",(0,a.jsx)(n.p,{children:"Let's move some of the user logic into a custom repository. First, let's define the repository. We'll include a method to check if an email exists, and to login users:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/user.repository.ts"',children:"import { EntityRepository } from '@mikro-orm/mysql';\nimport { User } from './user.entity.js';\nimport { AuthError } from '../common/auth.error.js';\nimport { Password } from './password.runtimeType.js';\n\nconst emptyHash = await Password.fromRaw('');\n\nexport class UserRepository extends EntityRepository<User> {\n\n  async exists(email: string) {\n    return (await this.count({ email })) > 0;\n  }\n\n  async login(email: string, password: string) {\n    const err = new AuthError('Invalid combination of email and password');\n    if (password === '' || email === '') {\n      throw err;\n    }\n\n    const user = await this.findOne({ email }, {\n        populate: ['password'], // password is a lazy property, we need to populate it\n      })\n      // On failure, we return a pseudo user with an empty password hash.\n      // This approach minimizes the effectiveness of timing attacks\n      ?? { password: emptyHash, id: 0, token: undefined };\n\n    if (await user.password.verifyAndMaybeRehash(password)) {\n      await this.getEntityManager().flush();\n      return user;//password is a hidden property, so it won't be returned, even on success.\n    }\n\n    throw err;\n  }\n}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Next, we'll need to associate this repository with the user entity on the entity's side. To do that in a \"schema first\" approach, you need to fill in the ",(0,a.jsx)(n.code,{children:"repositoryClass"})," property in extension hooks."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/mikro-orm.config.ts"',children:"+          case 'UserRepository':\n+              return `user/user.repository`;\n          case 'User':\n              return `user/${entityName.toLowerCase()}.entity`;\n...\n          const userEntity = metadata.find(meta => meta.className === 'User');\n          if (userEntity) {\n+            userEntity.repositoryClass = 'UserRepository';\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"and regenerate the entities."}),"\n",(0,a.jsxs)(n.p,{children:["The entity's options will now include a factory for the repository class, as well as a TypeScript hint. To use the custom repository class when available, and fallback to the default ones when not, we should modify our database wrapper to use the ",(0,a.jsx)(n.code,{children:"GetRepository"})," type, like so:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/db.ts"',children:'import {\n    type EntityManager,\n    type EntityRepository,\n+    type GetRepository,\n    MikroORM,\n    type Options\n  } from "@mikro-orm/mysql";\n...\n\n  export interface Services {\n    orm: MikroORM;\n    em: EntityManager;\n-    user: EntityRepository<User>;\n-    article: EntityRepository<Article>;\n-    tag: EntityRepository<Tag>;\n-    comment: EntityRepository<Comment>;\n+    user: GetRepository<User, EntityRepository<User>>;\n+    article: GetRepository<Article, EntityRepository<Article>>;\n+    tag: GetRepository<Tag, EntityRepository<Tag>>;\n+    comment: GetRepository<Comment, EntityRepository<Comment>>;\n  }\n...\n'})}),"\n",(0,a.jsxs)(n.p,{children:["The second type argument to the ",(0,a.jsx)(n.code,{children:"GetRepository"})," type is a fallback class, in case the entity does not define a type hint. That fallback should match the class defined in the config as a default repository. We're using MikroORM's default, so we're just specifying that."]}),"\n",(0,a.jsxs)(n.p,{children:["Now that we have the repository defined and available, we can use it in ",(0,a.jsx)(n.code,{children:"user.routes.ts"}),", like so:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.routes.ts"',children:"...\n-const emptyHash = await Password.fromRaw('');\n...\n    app.post('/sign-up', async request => {\n      const body = request.body as EntityData<User, true>;\n  \n      if (!body.email || !body.fullName || !body.password) {\n        throw new Error('One of required fields is missing: email, fullName, password');\n      }\n  \n-      if ((await db.user.count({ email: body.email })) > 0) {\n+      if (await db.user.exists(body.email)) {\n        throw new Error('This email is already registered, maybe you want to sign in?');\n      }\n...\n    app.post('/sign-in', async request => {\n      const { email, password } = request.body as { email: string; password: string };\n-      const err = new AuthError('Invalid combination of email and password');\n-      if (password === '' || email === '') {\n-        throw err;\n-      }\n-  \n-      const user = await db.user.findOne({ email }, {\n-        populate: ['password'], // password is a lazy property, we need to populate it\n-      })\n-        // On failure, we return a pseudo user with an empty password hash.\n-        // This approach minimizes the effectiveness of timing attacks\n-        ?? { password: emptyHash, id: 0, token: undefined };\n-  \n-      if (await user.password.verifyAndMaybeRehash(password)) {\n-        await db.em.flush();\n-        user.token = app.jwt.sign({ id: user.id });\n-        return user;//password is a hidden property, so it won't be returned, even on success.\n-      }\n-  \n-      throw err;\n+      const user = await db.user.login(email, password);\n+      user.token = app.jwt.sign({ id: user.id });\n+      return user;\n    });\n"})}),"\n",(0,a.jsx)(n.h3,{id:"adding-input-runtime-validation-via-zod",children:"Adding input runtime validation via Zod"}),"\n",(0,a.jsxs)(n.p,{children:["Every time we do ",(0,a.jsx)(n.code,{children:"as"})," on something from ",(0,a.jsx)(n.code,{children:"request"}),', we are effectively telling TypeScript we know what the user input will be shaped like. In reality, nothing is stopping the user from submitting something not conforming to that shape, or not even inputting JSON in the first place. We should validate all user input (which in our case means anything from "request") before passing it further along in our logic. One good way to do that is using Zod. Let\'s add such validation.']}),"\n",(0,a.jsx)(n.p,{children:"Install Zod:"}),"\n",(0,a.jsxs)(s.A,{groupId:"npm2yarn",children:[(0,a.jsx)(o.A,{value:"npm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"npm install zod\n"})})}),(0,a.jsx)(o.A,{value:"yarn",label:"Yarn",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"yarn add zod\n"})})}),(0,a.jsx)(o.A,{value:"pnpm",label:"pnpm",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"pnpm add zod\n"})})}),(0,a.jsx)(o.A,{value:"bun",label:"Bun",children:(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"bun add zod\n"})})})]}),"\n",(0,a.jsx)(n.p,{children:"First off, let's deal with the sign-in endpoint."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user.routes.ts"',children:"...\n+import { z } from 'zod';\n+\n...\n+    const signInPayload = z.object({\n+      email: z.string().min(1),\n+      password: z.string().min(1),\n+    });\n+\n    app.post('/sign-in', async request => {\n-      const { email, password } = request.body as { email: string; password: string };\n+      const { email, password } = signInPayload.parse(request.body);\n...\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Zod includes a validator for syntax validity of email, but we don't need it during sign-in. As long as the email is not empty, we can search it. If the email is not valid, it won't exist in the database to begin with. We'll make sure of that during the sign-up. Let's do that now, and while we're at it, let's automatically hash the password after validation, to simplify the call to the ",(0,a.jsx)(n.code,{children:"create()"})," method:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user.routes.ts"',children:"...\n+  const signUpPayload = z.object({\n+    email: z.string().email(),\n+    password: z\n+      .string()\n+      .min(1)\n+      .transform(async (raw) => Password.fromRaw(raw)),\n+    fullName: z.string().min(1),\n+    bio: z.string().optional().default(''),\n+  });\n+\n  app.post('/sign-up', async request => {\n-    const body = request.body as EntityData<User, true>;\n-\n-    if (!body.email || !body.fullName || !body.password) {\n-      throw new Error('One of required fields is missing: email, fullName, password');\n-    }\n-\n-    if ((await db.user.count({ email: body.email })) > 0) {\n-      throw new Error('This email is already registered, maybe you want to sign in?');\n-    }\n-\n-    const user = db.user.create({\n-      fullName: body.fullName,\n-      email: body.email,\n-      password: await Password.fromRaw(body.password),\n-      bio: body.bio ?? '',\n-    });\n+    const body = await signUpPayload.parseAsync(request.body);\n+\n+    if (await db.user.exists(body.email)) {\n+      throw new Error('This email is already registered, maybe you want to sign in?');\n+    }\n+\n+    const user = db.user.create(body);\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"You could add a check constraint for that instead (or in addition to Zod), but the check constraint would be applied later, after we spend time to hash the new password. To save time and server resources on long creation procedures like that, you should include as much validation as you can, as early as you can, like we did here."}),"\n",(0,a.jsxs)(n.p,{children:["Finally, let's add some validation for the query string in ",(0,a.jsx)(n.code,{children:"article.routes.ts"}),". Unlike our sign-up and sign-in validator, there's a high chance we'll want to do paging in multiple places (e.g. in a list of users), so we should define our validator in a dedicated file."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/common/validators.ts"',children:"import { z } from 'zod';\n\nexport const pagingParams = z.object({\n    limit: z.number().int().positive().optional(),\n    offset: z.number().int().nonnegative().optional(),\n});\n"})}),"\n",(0,a.jsx)(n.p,{children:'And now let\'s use it at the "/article" endpoint:'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/article/article.routes.ts"',children:"import { type Services } from '../../db.js';\n+import { pagingParams } from '../common/validators.js';\nexport default (async (app, { db }) => {\n  app.get('/', async (request) => {\n-    const { limit, offset } = request.query as {\n-      limit?: number;\n-      offset?: number;\n-    };\n+    const { limit, offset } = pagingParams.parse(request.query);\n...\n"})}),"\n",(0,a.jsx)(n.h2,{id:"making-backwards-compatible-changes-to-the-database",children:"Making backwards compatible changes to the database"}),"\n",(0,a.jsx)(n.p,{children:"Near the end there, you may have noticed that we still had to check whether the user's email exists before adding them to the database. On a busy server however, it's possible for a user to be added right in between our check and the flush of the new user. Further, if we had many users, we would need to do a linear search on the table, as there's no index on the email column. We can add one, and we should make it unique to prevent double insertion on a busy server."}),"\n",(0,a.jsx)(n.p,{children:"Let's try to generate a new migration for that."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/migrations/Migration00000000000002.ts"',children:"import { Migration } from '@mikro-orm/migrations';\n\nexport class Migration00000000000002 extends Migration {\n\n  async up(): Promise<void> {\n    await this.execute(`\n      ALTER TABLE \\`blog\\`.\\`users\\` \n        ADD UNIQUE INDEX \\`email_UNIQUE\\` (\\`email\\` ASC) VISIBLE;\n    `);\n  }\n\n  async down(): Promise<void> {\n    await this.execute(`\n      ALTER TABLE \\`blog\\`.\\`users\\` \n        DROP INDEX \\`email_UNIQUE\\` ;\n    `);\n  }\n\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"Because this migration is fully backwards compatible, and we are automatically running migrations during startup, we could deploy our code without regenerating the entities even. However, we should do that anyway, since the entity definitions have changed as a result of this."}),"\n",(0,a.jsxs)(n.p,{children:["After this migration is executed, we may now output a custom error when that unique constraint is violated. Note that we should still keep the application level check, performed before the ",(0,a.jsx)(n.code,{children:"create"})," attempt. Attempting to insert will consume the auto increment ID even on unique constraint violations, so to prevent its early exhaustion, we should check in advance as well."]}),"\n",(0,a.jsx)(n.p,{children:"Let's add that custom error class first:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/duplicate.error.ts"',children:"export class DuplicateUserError extends Error {}\n"})}),"\n",(0,a.jsx)(n.p,{children:"And then wrap violations of the unique constraint on sign-up:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.routes.ts"',children:"...\n+import { DuplicateUserError } from './duplicate.error.js';\n...\n\n  // register new user\n  app.post('/sign-up', async request => {\n    const body = await signUpValidator.parseAsync(request.body);\n    if (await db.user.exists(body.email)) {\n-      throw new Error('This email is already registered, maybe you want to sign in?');\n+      throw new DuplicateUserError('This email is already registered, maybe you want to sign in?');\n    }\n    const user = db.user.create(body);\n      \n-    await db.em.persist(user).flush();\n-\n-    // after flush, we have the `user.id` set\n-    console.log(`User ${user.id} created`);\n-\n-    user.token = app.jwt.sign({ id: user.id });\n+    try {\n+      await db.em.persist(user).flush();\n+  \n+      // after flush, we have the `user.id` set\n+      console.log(`User ${user.id} created`);\n+      \n+      user.token = app.jwt.sign({ id: user.id });\n+    \n+      return user;\n+    } catch (e: unknown) {\n+      if (e instanceof UniqueConstraintViolationException) {\n+        throw new DuplicateUserError(\n+          'This email is already registered, maybe you want to sign in?',\n+          { cause: e },\n+        );\n+      }\n+      throw e;\n+    }\n  });\n...\n"})}),"\n",(0,a.jsxs)(n.p,{children:["And finally, we can return a different status code on this error again. Status ",(0,a.jsx)(n.code,{children:"409 Conflict"})," seems like the most appropriate."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/common/hooks.ts"',children:"...\nimport { AuthError } from './auth.error.js';\n+import { DuplicateUserError } from '../user/duplicate.error.js';\n...\n  app.setErrorHandler((error, request, reply) => {\n    if (error instanceof AuthError) {\n      return reply.status(401).send(error);\n    }\n  \n    // we also handle not found errors automatically\n    // `NotFoundError` is an error thrown by the ORM via `em.findOneOrFail()` method\n    if (error instanceof NotFoundError) {\n      return reply.status(404).send(error);\n    }\n+\n+    if (error instanceof DuplicateUserError) {\n+      return reply.status(409).send(error);\n+    }\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"We can now adjust our test accordingly:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="test/user.test.ts"',children:"...\n-  expect(res1dup.statusCode).toBe(500);\n+  expect(res1dup.statusCode).toBe(409);\n  expect(res1dup.json()).toMatchObject({\n    message: 'This email is already registered, maybe you want to sign in?',\n  });\n...\n"})}),"\n",(0,a.jsx)(n.admonition,{title:"Exercise",type:"info",children:(0,a.jsxs)(n.p,{children:["You can try to generalize this to work for any unique constraint violation by analyzing the ",(0,a.jsx)(n.code,{children:"sqlMessage"})," property of ",(0,a.jsx)(n.code,{children:"UniqueConstraintViolationException"})," and searching the metadata based on the parsing results. You can then produce error messages that point the client to the one or multiple properties that causes a duplicate entry. Doing so will tie you to your SQL driver, and possibly even database engine version, so if you go on this route, you should do so with care. Do unit tests for your parsing in addition to your error conditions themselves, and make sure to run all tests after an upgrade of the database. If the error messages have changed, you will want to support parsing both forms until your production database is updated."]})}),"\n",(0,a.jsx)(n.h3,{id:"reusing-the-user-authentication-check",children:"Reusing the user authentication check"}),"\n",(0,a.jsx)(n.p,{children:'We have the "/profile" endpoint, where we check whether the user is authenticated and return it if it is. For the sake of other endpoints that need to be authenticated, we should extract this into a function that will either give us the current user, or throw.'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/utils.ts"',children:"import { FastifyRequest } from 'fastify';\nimport { type User } from '../user/user.entity.js';\nimport { AuthError } from './auth.error.ts';\n\nexport function getUserFromToken(req: FastifyRequest): User {\n  if (!req.user) {\n    throw new AuthError('Please provide your token via Authorization header');\n  }\n\n  return req.user as User;\n}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["and we can already adjust the ",(0,a.jsx)(n.code,{children:"user.routes.ts"})," file to use it:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.routes.ts"',children:"...\n-import { AuthError } from '../common/auth.error.js';\n+import { getUserFromToken } from '../common/utils.js';\n...\n  app.get('/profile', async request => {\n-    if (!request.user) {\n-      throw new AuthError('Please provide your token via Authorization header');\n-    }\n-  \n-    return request.user as User;\n+    return getUserFromToken(request);\n  });\n"})}),"\n",(0,a.jsx)(n.h3,{id:"modularizing-the-configuration",children:"Modularizing the configuration"}),"\n",(0,a.jsxs)(n.p,{children:["Our ",(0,a.jsx)(n.code,{children:"mikro-orm.config.ts"}),' file has already grown quite a lot, and grew a bit even between "Checkpoint 2" and now. As the number of your entities and the number of modifications you may wish to do grows, you may need to put related modifications into dedicated files, and just let ',(0,a.jsx)(n.code,{children:"mikro-orm.config.ts"})," collect and apply them. Exactly how you do that depends on your project and your needs."]}),"\n",(0,a.jsxs)(n.p,{children:["We will implement an organization similar to what we have been doing so far, and create files per module with all module related modifications, under a suffix to denote its purpose. Let's use the suffix ",(0,a.jsx)(n.code,{children:"*.gen.ts"}),". Each such file will have a default export that is of type ",(0,a.jsx)(n.code,{children:"GenerateOptions"}),". For each method, we will apply that method, if defined. We'll add a special case the empty string return value of ",(0,a.jsx)(n.code,{children:"fileName"}),' from our files to mean "Don\'t use this result, try next". If we were to do that in the main config, the entity generator will happily create a file with no name and ',(0,a.jsx)(n.code,{children:".ts"})," extension in the base folder, but we know we don't need that."]}),"\n",(0,a.jsx)(n.p,{children:"So, let's add"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/user.gen.ts"',children:"import type { GenerateOptions } from \"@mikro-orm/core\";\n\nconst settings: GenerateOptions = {\n  fileName: (entityName) => {\n    switch (entityName) {\n    case 'UserRepository':\n      return `user/user.repository`;\n    case 'User':\n      return `user/${entityName.toLowerCase()}.entity`;\n    case 'Password':\n      return `user/password.runtimeType`;\n    case 'PasswordType':\n      return `user/password.type`;\n    }\n    return '';\n  },\n  onInitialMetadata: (metadata, platform) => {\n    const userEntity = metadata.find(meta => meta.className === 'User');\n    if (userEntity) {\n      userEntity.repositoryClass = 'UserRepository';\n      userEntity.addProperty({\n        persist: false,\n        name: 'token',\n        nullable: true,\n        default: null,\n        defaultRaw: 'null',\n        fieldNames: [platform.getConfig().getNamingStrategy().propertyToColumnName('token')],\n        columnTypes: ['varchar(255)'],\n        type: 'string',\n        runtimeType: 'string',\n      });\n      const passwordProp = userEntity.properties.password;\n      passwordProp.hidden = true;\n      passwordProp.lazy = true;\n      passwordProp.type = 'PasswordType';\n      passwordProp.runtimeType = 'Password';\n    }\n  }\n};\nexport default settings;\n"})}),"\n",(0,a.jsx)(n.p,{children:"and"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.gen.ts"',children:"import type { GenerateOptions } from \"@mikro-orm/core\";\n\nconst settings: GenerateOptions = {\n  fileName: (entityName) => {\n    switch (entityName) {\n    case '_Article':\n      return `article/article.entity`;\n    case 'Article':\n      return `article/article.customEntity`;\n    case 'ArticleTag':\n    case 'Tag':\n    case 'Comment':\n      return `article/${entityName.toLowerCase()}.entity`;\n    }\n    return '';\n  },\n  onInitialMetadata: (metadata, platform) => {\n    const articleEntity = metadata.find(meta => meta.className === 'Article');\n    if (articleEntity) {\n      const textProp = articleEntity.properties.text;\n      textProp.lazy = true;\n    }\n  },\n  onProcessedMetadata: (metadata, platform) => {\n    const articleEntity = metadata.find(meta => meta.className === 'Article');\n    if (articleEntity) {\n      articleEntity.className = '_Article';\n      articleEntity.abstract = true;\n    }\n  },\n};\nexport default settings;\n"})}),"\n",(0,a.jsxs)(n.p,{children:["And finally, let's hook them up in our config. We'll use ",(0,a.jsx)(n.code,{children:"globby"})," to match all ",(0,a.jsx)(n.code,{children:"*.gen.ts"})," files relative to the config itself. We're using ",(0,a.jsx)(n.code,{children:"globby"}),", because it is already present - it is what MikroORM uses when searching for entities by a path. We'll pre-filter the results at the top, so that the actual entity processing is faster. Our full config is thus:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/mikro-orm.config.ts"',children:"import {\n  defineConfig,\n  type MikroORMOptions,\n} from '@mikro-orm/mysql';\nimport { UnderscoreNamingStrategy, type GenerateOptions } from '@mikro-orm/core';\nimport { Migrator } from '@mikro-orm/migrations';\nimport pluralize from 'pluralize';\nimport { join, dirname } from 'node:path';\nimport { sync } from 'globby';\nimport { fileURLToPath } from 'node:url';\n\nconst isInMikroOrmCli = process.argv[1]?.endsWith(join('@mikro-orm', 'cli', 'esm')) ?? false;\nconst isRunningGenerateEntities = isInMikroOrmCli && process.argv[2] === 'generate-entities';\n\nconst mikroOrmExtensions: MikroORMOptions['extensions'] = [Migrator];\n\nconst fileNameFunctions: NonNullable<GenerateOptions['fileName']>[] = [];\nconst onInitialMetadataFunctions: NonNullable<GenerateOptions['onInitialMetadata']>[] = [];\nconst onProcessedMetadataFunctions: NonNullable<GenerateOptions['onProcessedMetadata']>[] = [];\n\nif (isInMikroOrmCli) {\n  mikroOrmExtensions.push((await import('@mikro-orm/entity-generator')).EntityGenerator);\n  if (isRunningGenerateEntities) {\n    const fileDir = dirname(fileURLToPath(import.meta.url));\n    const genExtensionFiles = sync('./modules/**/*.gen.ts', { cwd: fileDir });\n    for (const file of genExtensionFiles) {\n      const genExtension = (await import(file)).default as GenerateOptions;\n      if (genExtension.fileName) {\n        fileNameFunctions.push(genExtension.fileName);\n      }\n      if (genExtension.onInitialMetadata) {\n        onInitialMetadataFunctions.push(genExtension.onInitialMetadata);\n      }\n      if (genExtension.onProcessedMetadata) {\n        onProcessedMetadataFunctions.push(genExtension.onProcessedMetadata);\n      }\n    }\n  }\n}\n\nexport default defineConfig({\n  extensions: mikroOrmExtensions,\n  multipleStatements: isInMikroOrmCli,\n  discovery: {\n    warnWhenNoEntities: !isInMikroOrmCli,\n  },\n  entities: isRunningGenerateEntities ? [] : ['dist/**/*.customEntity.js', 'dist/**/*.entity.js'],\n  entitiesTs: isRunningGenerateEntities ? [] : ['src/**/*.customEntity.ts', 'src/**/*.entity.ts'],\n  host: 'localhost',\n  user: 'root',\n  password: '',\n  dbName: 'blog',\n  // enable debug mode to log SQL queries and discovery information\n  debug: true,\n  migrations: {\n      path: 'dist/migrations',\n      pathTs: 'src/migrations',\n  },\n  namingStrategy: class extends UnderscoreNamingStrategy {\n    override getEntityName(tableName: string, schemaName?: string): string {\n      return pluralize.singular(super.getEntityName(tableName, schemaName));\n    }\n  },\n  entityGenerator: {\n    scalarTypeInDecorator: true,\n    fileName: (entityName) => {\n      for (const f of fileNameFunctions) {\n        const r = f(entityName);\n        if (r === '') {\n          continue;\n        }\n        return r;\n      }\n      return `common/${entityName.toLowerCase()}.entity`;\n    },\n    onInitialMetadata: (metadata, platform) => {\n      return Promise.all(onInitialMetadataFunctions.map(f => f(metadata, platform))).then();\n    },\n    onProcessedMetadata: (metadata, platform) => {\n      return Promise.all(onProcessedMetadataFunctions.map(f => f(metadata, platform))).then();\n    },\n    save: true,\n    path: 'src/modules',\n    esmImport: true,\n    outputPurePivotTables: true,\n    readOnlyPivotTables: true,\n    bidirectionalRelations: true,\n    customBaseEntityName: 'Base',\n    useCoreBaseEntity: true,\n  },\n});\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Regeneration at this point should produce results no different from what we've had so far. But you can now add extra ",(0,a.jsx)(n.code,{children:"*.gen.ts"})," files, each modifying some aspect of some entities."]}),"\n",(0,a.jsx)(n.admonition,{title:"Exercise",type:"info",children:(0,a.jsxs)(n.p,{children:["You can try to implement a different pattern for handling the ",(0,a.jsx)(n.code,{children:"*.gen.ts"})," files, such as accepting the metadata of a given entity based on the table name, and register file name entries as needed during those extensions. By the time ",(0,a.jsx)(n.code,{children:"fileName"})," is first called, ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," and ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"})," have already finished executing, so they can determine its behavior. Doing this is probably overkill for most cases, but it may be helpful if you have extensions that you want to apply on different schemas, not just one you are fully in control of."]})}),"\n",(0,a.jsx)(n.h2,{id:"-checkpoint-3",children:"\u26F3 Checkpoint 3"}),"\n",(0,a.jsx)(n.p,{children:"Our application is now structured like an enterprise level application, ready for further modules or further additions to the existing modules. We even made another migration along the way. We are now ready to add more features."}),"\n",(0,a.jsx)(n.h2,{id:"completing-the-project",children:"Completing the project"}),"\n",(0,a.jsx)(n.h3,{id:"add-the-remaining-article-endpoints",children:"Add the remaining article endpoints"}),"\n",(0,a.jsx)(n.p,{children:"Let's add the remaining article endpoints."}),"\n",(0,a.jsx)(n.p,{children:"Let's start with one about viewing an article by slug and adding a comment:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.routes.ts"',children:"// rest of the code\n\n  const articleBySlugParams = z.object({\n    slug: z.string().min(1),\n  });\n  \n  app.get('/:slug', async request => {\n    const { slug } = articleBySlugParams.parse(request.params);\n    return db.article.findOneOrFail({ slug }, {\n      populate: ['author', 'commentCollection.author', 'text'],\n    });\n  });\n\n  const articleCommentPayload = z.object({\n    text: z.string().min(1),\n  });\n  \n  app.post('/:slug/comment', async request => {\n    const { slug } = articleBySlugParams.parse(request.params);\n    const { text } = articleCommentPayload.parse(request.body);\n    const author = getUserFromToken(request);\n    const article = await db.article.findOneOrFail({ slug });\n    const comment = db.comment.create({ author, article, text });\n  \n    // We can add the comment to `article.comments` collection,\n    // but in fact it is a no-op, as it will be automatically\n    // propagated by setting Comment.author property.\n    article.commentCollection.add(comment);\n  \n    // mention we don't need to persist anything explicitly\n    await db.em.flush();\n  \n    return comment;\n  });\n\n// rest of the code\n"})}),"\n",(0,a.jsxs)(n.p,{children:['If you compare the code with the equivalent from the "code first" guide, you will notice that we\'ve added Zod for some basic validation. Also, the generator used the name "commentCollection" to represent the relation to the "comments" table. The default is formed from the entity name, combined with the suffix "Collection" for 1',":N",' relations, or "Inverse" for M',":N"," relations. We could adjust that in the naming strategy by overriding ",(0,a.jsx)(n.code,{children:"inverseSideName"}),' if we\'d like (e.g. by taking the entity name and converting it to plural with pluralize), but to avoid potential conflicts with properties defined in the table itself, let\'s keep it as is. We\'re more likely to name a column with the plural form of an entity than we are to name it with "_collection" or "_inverse" at the end, making conflicts less likely in their current form.']}),"\n",(0,a.jsx)(n.p,{children:"Next, let's try to add the article creation endpoint:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.routes.ts"',children:"// rest of the code above\n\n  const newArticlePayload = z.object({\n    title: z.string().min(1),\n    text: z.string().min(1),\n    description: z.string().min(1).optional(),\n  });\n\n  app.post('/', async request => {\n    const { title, text, description } = newArticlePayload.parse(request.body);\n    const author = getUserFromToken(request);\n    const article = db.article.create({\n      title,\n      text,\n      author,\n      description,\n    });\n  \n    await db.em.flush();\n  \n    return article;\n  });\n// rest of the code\n"})}),"\n",(0,a.jsx)(n.p,{children:"You should be seeing a type error. This is because our entity declares slug and description as required properties. There are three solutions here. The first possible solution is to use the article constructor directly and persist the new entity."}),"\n",(0,a.jsx)(n.p,{children:"The second is to create a custom entity repository for article, in which we override the create method or add a custom one that calls the constructor and persists the new entity. We'll skip showing these solutions."}),"\n",(0,a.jsx)(n.admonition,{title:"Exercise",type:"info",children:(0,a.jsx)(n.p,{children:"Try to implement these solutions as well. Step back as soon as you can build the application."})}),"\n",(0,a.jsx)(n.p,{children:"And the third one is to declare those properties as optional. The best way to do that is to declare them as optional in our mapped superclass."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/article/article.customEntity.ts"',children:"-import { Entity, type Rel } from '@mikro-orm/core';\n+import { Entity, OptionalProps, type Rel } from '@mikro-orm/core';\nimport { _Article } from './article.entity.js';\nimport { User } from '../user/user.entity.js';\n\nfunction convertToSlug(text: string) {\n  return text\n    .toLowerCase()\n    .replace(/[^\\w ]+/g, '')\n    .replace(/ +/g, '-');\n}\n\n@Entity({ tableName: 'articles' })\nexport class Article extends _Article {\n+\n+  [OptionalProps]?: 'slug' | 'description';\n\n  constructor(title: string, text: string, author: Rel<User>) {\n    super();\n    this.title = title;\n    this.text = text;\n    this.author = author;\n    this.slug = convertToSlug(title);\n    this.description = this.text.substring(0, 999) + '\u2026';\n  }\n\n}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Technically, we could declare them as optional in the base class by modifying that in ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," or ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"}),", but if we ever want to bypass the superclass for whatever reason, we will be prone to errors from the missing slug and description."]}),"\n",(0,a.jsx)(n.p,{children:"After the modifications to the mapped superclass, the code now compiles again."}),"\n",(0,a.jsxs)(n.p,{children:["For our next two endpoints, we'll want to ensure only the author of an article can update and delete it. The check itself is trivial, but let's make it so that we throw a separate error that results in ",(0,a.jsx)(n.code,{children:"403 Forbidden"})," if the user is different from the author of an article."]}),"\n",(0,a.jsx)(n.p,{children:"Let's add the error:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/disallowed.error.ts"',children:"export class DisallowedError extends Error {}\n"})}),"\n",(0,a.jsxs)(n.p,{children:["And add handling for it in ",(0,a.jsx)(n.code,{children:"hooks.ts"}),":"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/common/hooks.ts"',children:"...\nimport { AuthError } from './auth.error.js';\n+import { DisallowedError } from './disallowed.error.js';\n...\n  app.setErrorHandler((error, request, reply) => {\n    if (error instanceof AuthError) {\n      return reply.status(401).send(error);\n    }\n    \n+    if (error instanceof DisallowedError) {\n+      return reply.status(403).send(error);\n+    }\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"And we're now ready to add the article endpoints to update and remove and article by ID:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/article/article.routes.ts"',children:"// rest of the imports\nimport { DisallowedError } from '../common/disallowed.error.js';\nimport { wrap } from '@mikro-orm/mysql';\n\n// rest of the code\n\n  const articleByIdParams = z.object({\n    id: z.coerce.number().int().positive()\n  });\n  const updateArticlePayload = newArticlePayload.partial().extend({\n    slug: z.string().min(1).optional(),\n  });\n\n  app.patch('/:id', async request => {\n    const user = getUserFromToken(request);\n    const { id } = articleByIdParams.parse(request.params);\n    const article = await db.article.findOneOrFail(id);\n    if (article.author !== user) {\n      throw new DisallowedError('Only the author of an article is allowed to update it');\n    }\n\n    wrap(article).assign(updateArticlePayload.parse(request.body));\n    await db.em.flush();\n  \n    return article;\n  });\n\n  app.delete('/:id', async request => {\n    const user = getUserFromToken(request);\n    const { id } = articleByIdParams.parse(request.params);\n    const article = await db.article.findOneOrFail(id);\n    if (article.author !== user) {\n      throw new DisallowedError('Only the author of an article is allowed to delete it');\n    }\n\n    // mention `nativeDelete` alternative if we don't care about validations much\n    await db.em.remove(article).flush();\n  \n    return { success: true };\n  });\n// rest of the code\n"})}),"\n",(0,a.jsx)(n.p,{children:"Thanks to MikroORM's identity map, we can compare the objects like how we've done above."}),"\n",(0,a.jsx)(n.p,{children:"Let's also add an endpoint to update our user profile:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.routes.ts"',children:"...\n-import { UniqueConstraintViolationException } from '@mikro-orm/mysql';\n+import { UniqueConstraintViolationException, wrap } from '@mikro-orm/mysql';\n...\n+  const profileUpdatePayload = signUpPayload.partial();\n+  \n+  app.patch('/profile', async (request) => {\n+    const user = getUserFromToken(request);\n+    wrap(user).assign(profileUpdatePayload.parse(request.body));\n+    await db.em.flush();\n+    return user;\n+  });\n"})}),"\n",(0,a.jsx)(n.admonition,{title:"Exercise",type:"info",children:(0,a.jsx)(n.p,{children:"Add unit tests for those new endpoints."})}),"\n",(0,a.jsx)(n.h3,{id:"embeddable-entities",children:"Embeddable entities"}),"\n",(0,a.jsx)(n.p,{children:"MikroORM offers embeddable objects, which can serve one of two purposes."}),"\n",(0,a.jsxs)(n.ol,{children:["\n",(0,a.jsx)(n.li,{children:"Group related columns in a table under a property."}),"\n",(0,a.jsx)(n.li,{children:"Provide a more entity-like experience to querying JSON columns."}),"\n"]}),"\n",(0,a.jsx)(n.p,{children:"The entity generator is powerful enough to output such entities, when they are encoded in the metadata. However, we need to heavily alter the metadata to add new embeddable entities and add references to them. It is also perfectly valid to write embeddable entities manually, and just add references to them during entity generation.  We'll explore both types of embeddables and both ways of generating them."}),"\n",(0,a.jsx)(n.h4,{id:"embeddable-as-a-group-of-columns",children:"Embeddable as a group of columns"}),"\n",(0,a.jsx)(n.p,{children:'First, for the grouping of columns. In most of our entities, we have "created_at" and "updated_at" columns, but not quite all of them (case in point: the pivot tables). Let\'s make it a policy to add an optional "_track" property to any entity with such columns. That property will be an embeddable object having those two fields. We\'ll also remove them from their original properties, keeping only the copy in the embeddable object. For simplicity, we\'ll assume the type and defaults of all such columns are correct.'}),"\n",(0,a.jsxs)(n.p,{children:['Normally, embeddable objects map to a column formed by using the property as a prefix. In our case, that would be "track_created_at" and "track_updated_at". We don\'t want that, so we will set the ',(0,a.jsx)(n.code,{children:"prefix"})," option to ",(0,a.jsx)(n.code,{children:"false"}),", so that in the end, we still map to ",(0,a.jsx)(n.code,{children:"created_at"})," and ",(0,a.jsx)(n.code,{children:"updated_at"}),"."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/common/track.gen.ts"',children:"import { EntityMetadata, ReferenceKind, type GenerateOptions } from '@mikro-orm/core';\n\nconst settings: GenerateOptions = {\n  onInitialMetadata: (metadata, platform) => {\n    for (const meta of metadata) {\n      if (\n          typeof meta.properties.createdAt !== 'undefined' &&\n          typeof meta.properties.updatedAt !== 'undefined'\n      ) {\n        meta.removeProperty('createdAt', false);\n        meta.removeProperty('updatedAt', false);\n        meta.addProperty(\n          {\n            name: '_track',\n            kind: ReferenceKind.EMBEDDED,\n            optional: true,\n            nullable: true,\n            type: 'Track',\n            runtimeType: 'Track',\n            prefix: false,\n            object: false,\n          },\n          false,\n        );\n        meta.sync();\n      }\n    }\n\n    const trackClass = new EntityMetadata({\n      className: 'Track',\n      tableName: 'track',\n      embeddable: true,\n      relations: [],\n    });\n    trackClass.addProperty(\n      {\n        name: 'createdAt',\n        fieldNames: ['created_at'],\n        columnTypes: ['datetime'],\n        type: 'datetime',\n        runtimeType: 'Date',\n        defaultRaw: 'CURRENT_TIMESTAMP',\n      },\n      false,\n    );\n    trackClass.addProperty(\n      {\n        name: 'updatedAt',\n        fieldNames: ['updated_at'],\n        columnTypes: ['datetime'],\n        type: 'datetime',\n        runtimeType: 'Date',\n        defaultRaw: 'CURRENT_TIMESTAMP',\n      },\n      false,\n    );\n    trackClass.sync();\n\n    metadata.push(trackClass);\n  },\n};\nexport default settings;\n"})}),"\n",(0,a.jsxs)(n.p,{children:['If you regenerate the entities now, you\'ll see "src/modules/common/track.entity.ts" created, and other classes are now referencing it. Since we are creating the class dynamically, we can keep it saved with an ',(0,a.jsx)(n.code,{children:"*.entity.ts"})," extension."]}),"\n",(0,a.jsx)(n.admonition,{type:"warning",children:(0,a.jsx)(n.p,{children:"When working on bigger projects and doing similar modifications, you should do extra checks on the column type, nullability, and default value. Take action to group columns only when all of their metadata lines up with what you have in the embeddable. Otherwise, leave the properties alone. Mistakes can happen during the authoring of migrations. Your entity generation extensions can (and should) be made resilient towards such mistakes. Not getting the modification in the output when you expect it will alert you that there is such a mistake."})}),"\n",(0,a.jsx)(n.h4,{id:"embeddable-as-a-type-of-json-column",children:"Embeddable as a type of JSON column"}),"\n",(0,a.jsx)(n.p,{children:"We explored custom types already, and can use them for JSON columns as well. But doing so means you opt out from MikroORM assisted queries to properties within the JSON. With a custom type, the JSON column is just a random object as far as MikroORM is concerned, and you only get to deal with it as an object after having fetched it. You can always write queries to JSON properties, of course (it's still a JSON column), but there will be no auto complete for object member names in your IDE within MikroORM calls. Not so with embeddable properties."}),"\n",(0,a.jsx)(n.p,{children:"Our schema is currently lacking any JSON properties. Let's add one. We can use one to store social media accounts of users, for example."}),"\n",(0,a.jsx)(n.p,{children:'Let\'s start by adding migration and regenerating our entities to include the "social" property.'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/migrations/Migration00000000000003.ts"',children:"import { Migration } from '@mikro-orm/migrations';\n\nexport class Migration00000000000003 extends Migration {\n\n  async up(): Promise<void> {\n    await this.execute(`\n      ALTER TABLE \\`users\\`\n        ADD COLUMN \\`social\\` JSON NULL DEFAULT NULL AFTER \\`bio\\`;\n    `);\n  }\n\n  async down(): Promise<void> {\n    await this.execute(`\n      ALTER TABLE \\`users\\`\n        DROP COLUMN \\`social\\`;\n    `);\n  }\n\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:'Execute the migration and regenerate the entities. You should see the "social" column defined with runtime type "any". OK, time to add the embeddable.'}),"\n",(0,a.jsx)(n.p,{children:"We can define the embeddable class manually:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-ts",metastring:'title="src/modules/user/social.customEntity.ts"',children:"import { Embeddable, Property, type Opt } from \"@mikro-orm/mysql\";\n\n@Embeddable()\nexport class Social {\n\n  @Property({ type: 'string' })\n  twitter!: string & Opt;\n\n  @Property({ type: 'string' })\n  facebook!: string & Opt;\n\n  @Property({ type: 'string' })\n  linkedin!: string & Opt;\n\n}\n"})}),"\n",(0,a.jsx)(n.p,{children:"In fairness, this class is simple enough that we may as well define it dynamically. But if you'd like to add helper methods (e.g. for getting the full link, our of just a username), you may want to define it manually."}),"\n",(0,a.jsxs)(n.p,{children:["Now, let's modify our ",(0,a.jsx)(n.code,{children:"user.gen.ts"})," to reference the embeddable:"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.gen.ts"',children:"-import { type GenerateOptions } from \"@mikro-orm/core\";\n+import { ReferenceKind, type GenerateOptions } from \"@mikro-orm/core\";\n...\n    case 'PasswordType':\n      return `user/password.type`;\n+    case 'Social':\n+      return `user/social.customEntity`;\n...\n      passwordProp.runtimeType = 'Password';\n+\n+      const socialProp = userEntity.properties.social;\n+      socialProp.kind = ReferenceKind.EMBEDDED;\n+      socialProp.type = 'Social';\n+      socialProp.prefix = false;\n+      socialProp.object = true;\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:'Regenerating the entities again, we now have the embeddable representing the contents of the JSON column. Just as with column groups, there is a prefix for the related JSON properties, but by setting "prefix" to "false", we can ensure the props in our entities map to the same properties in the JSON column. And the "object" option being set to "true" is how we set that property to represent a JSON column, rather than a group of columns.'}),"\n",(0,a.jsx)(n.admonition,{title:"Exercise",type:"info",children:(0,a.jsx)(n.p,{children:"Try to add a check constraint to the JSON column too. The embeddable helps ensure your application won't get exposed to unknown properties, or be able to enter unknown properties into the database. However, direct queries to your database may insert objects that won't have the required shape. Worse still, they may set the same properties, but with a different data type inside. That may ultimately crash your application if read out. A check constraint that at least checks the known properties will remove any possibility of that."})}),"\n",(0,a.jsx)(n.p,{children:"We should update our user endpoints to accept this new property:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/user/user.routes.ts"',children:"  const signUpPayload = z.object({\n    email: z.string().email(),\n    password: z\n      .string()\n      .min(1)\n      .transform(async (raw) => Password.fromRaw(raw)),\n    fullName: z.string().min(1),\n    bio: z.string().optional().default(''),\n+    social: z\n+      .object({\n+        twitter: z.string().min(1).optional(),\n+        facebook: z.string().min(1).optional(),\n+        linkedin: z.string().min(1).optional(),\n+      })\n+      .optional(),\n  });\n"})}),"\n",(0,a.jsx)(n.h3,{id:"formula-properties",children:"Formula properties"}),"\n",(0,a.jsxs)(n.p,{children:["Sometimes, you want to include some dynamic data per row. Generated columns can help when all the data you need is in the row, but what about when you want to include something non-deterministic (like the current time) or something from other tables (an aggregation perhaps) as the value? In SQL, this can be done with a subquery in your ",(0,a.jsx)(n.code,{children:"SELECT"})," clause. MikroORM allows you to define such subqueries as ",(0,a.jsx)(n.code,{children:"@Formula"})," decorated properties. These are not inferred from the database, but you can add such with ",(0,a.jsx)(n.code,{children:"onInitialMetadata"})," and ",(0,a.jsx)(n.code,{children:"onProcessedMetadata"}),"."]}),"\n",(0,a.jsx)(n.p,{children:"Let's add a count of comments to the article listing in that fashion. We'll make the property lazy, so that we don't necessarily compute it every time we get an article."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/article/article.gen.ts"',children:"...\n      textProp.lazy = true;\n+\n+      articleEntity.addProperty({\n+        name: 'commentsCount',\n+        fieldNames: ['comments_count'],\n+        columnTypes: ['INT'],\n+        unsigned: true,\n+        optional: true,\n+        type: 'integer',\n+        runtimeType: 'number',\n+        default: 0,\n+        lazy: true,\n+        formula: (alias) => `(SELECT COUNT(*) FROM comments WHERE article = ${alias}.id)`,\n+      });\n...\n"})}),"\n",(0,a.jsx)(n.p,{children:"And let's regenerate the entities. Lastly, let's ensure we add it to the listing:"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/article/article.routes.ts"',children:"...\n    const [items, total] = await db.article.findAndCount(\n      {},\n      {\n+        populate: ['commentsCount'],\n        limit,\n        offset,\n      },\n    );\n...\n"})}),"\n",(0,a.jsx)(n.h2,{id:"using-the-query-builder-during-entity-generation",children:"Using the query builder during entity generation"}),"\n",(0,a.jsx)(n.p,{children:"Notice that we had to write the full query as a string. So what happens if we rename the columns in the future? No entity generation or build errors of any kind. We would only get an error at runtime, if we populate the property. That's not good. We can remedy that by constructing the query dynamically based on the metadata, and do the final replacement of the alias at the end. This will ensure we get errors during entity generation if we've renamed the involved table or columns."}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-diff",metastring:'title="src/modules/article/article.gen.ts"',children:"import type { GenerateOptions } from '@mikro-orm/core';\n+import { type SqlEntityManager, Utils } from '@mikro-orm/mysql';\n...\n      textProp.lazy = true;\n\n+      const commentEntity = metadata.find((meta) => meta.className === 'Comment');\n+      if (!commentEntity) {\n+        return;\n+      }\n+      const em = (platform.getConfig().getDriver().createEntityManager() as SqlEntityManager);\n+      const qb = em.getKnex().queryBuilder().count().from(commentEntity.tableName).where(\n+        commentEntity.properties.article.fieldNames[0],\n+        '=',\n+        em.getKnex().raw('??.??', [em.getKnex().raw('??'), commentEntity.properties.id.fieldNames[0]])\n+      );\n+      const formula = Utils.createFunction(\n+        new Map(),\n+        `return (alias) => ${JSON.stringify(`(${qb.toSQL().sql})`)}.replaceAll('??', alias)`\n+      );\n+\n      articleEntity.addProperty({\n        name: 'commentsCount',\n        fieldNames: ['comments_count'],\n        columnTypes: ['INT'],\n        unsigned: true,\n        optional: true,\n        type: 'integer',\n        runtimeType: 'number',\n        default: 0,\n        lazy: true,\n-        formula: (alias) => `(SELECT COUNT(*) FROM comments WHERE article = ${alias}.id)`,\n+        formula,\n      });\n"})}),"\n",(0,a.jsxs)(n.p,{children:["If you regenerate the entities now, you'll see a slightly different function in the output from what we had before, but it still does the same job. With this in place, if we rename the involved columns from ",(0,a.jsx)(n.code,{children:"articles"})," or ",(0,a.jsx)(n.code,{children:"comments"}),", the entity generator would error. If we delete or rename the ",(0,a.jsx)(n.code,{children:"Comment"})," entity, generation would skip the commentsCount property, which would in turn create build errors if we were to reference it."]}),"\n",(0,a.jsx)(n.h2,{id:"deployment",children:"Deployment"}),"\n",(0,a.jsx)(n.h3,{id:"running-without-ts-node",children:"Running without ts-node"}),"\n",(0,a.jsx)(n.p,{children:'We have already added a "check" script to check our code without emitting anything. Let\'s actually emit our output, and run it with node rather than ts-node:'}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-json",metastring:'title="package.json"',children:'  "scripts": {\n    "build": "tsc --build",\n    "start:prod": "node ./dist/server.js",\n    ...\n  }\n'})}),"\n",(0,a.jsxs)(n.p,{children:['Because we already have "type" annotated everywhere, the application just works without further modifications. If you were to use a bundler instead of ',(0,a.jsx)(n.code,{children:"tsc"}),", you may need to do additional config. If the bundler is mangling your class names and property names (e.g. NextJS projects do that by default), you may adjust your naming strategy to always generate ",(0,a.jsx)(n.code,{children:"tableName"})," and ",(0,a.jsx)(n.code,{children:"fieldNames"})," options (e.g. by unconditionally returning an empty string in ",(0,a.jsx)(n.code,{children:"classToTableName"})," and ",(0,a.jsx)(n.code,{children:"propertyToColumnName"}),"), and regenerate your entities. This will ensure that no matter how the JS identifiers end up as in the production bundle, they will map to the correct tables and columns in your database."]}),"\n",(0,a.jsx)(n.h2,{id:"-checkpoint-4",children:"\u26F3 Checkpoint 4"}),"\n",(0,a.jsx)(n.p,{children:'Our application is fully ready to be deployed. You can always add more features, optimize performance in some areas, make error handling nicer, use "as" even less, and so on.'})]})}function p(e={}){let{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,a.jsx)(n,{...e,children:(0,a.jsx)(h,{...e})}):h(e)}},93703:function(e,n,t){t.d(n,{A:()=>i});var r=t(74848);t(96540);var a=t(39836);function i(e){let{children:n,hidden:t,className:i}=e;return(0,r.jsx)("div",{role:"tabpanel",className:(0,a.A)("tabItem_Ymn6",i),hidden:t,children:n})}},84109:function(e,n,t){t.d(n,{A:()=>j});var r=t(74848),a=t(96540),i=t(39836),s=t(16364),o=t(68251),l=t(56347),d=t(28004),c=t(25580),u=t(12213),h=t(75734);function p(e){var n,t;return null!=(t=null==(n=a.Children.toArray(e).filter(e=>"\n"!==e).map(e=>{if(!e||(0,a.isValidElement)(e)&&function(e){let{props:n}=e;return!!n&&"object"==typeof n&&"value"in n}(e))return e;throw Error("Docusaurus error: Bad <Tabs> child <".concat("string"==typeof e.type?e.type:e.type.name,'>: all children of the <Tabs> component should be <TabItem>, and every <TabItem> should have a unique "value" prop.'))}))?void 0:n.filter(Boolean))?t:[]}function m(e){let{value:n,tabValues:t}=e;return t.some(e=>e.value===n)}var g=t(98864);function f(e){for(var n=1;n<arguments.length;n++){var t=null!=arguments[n]?arguments[n]:{},r=Object.keys(t);"function"==typeof Object.getOwnPropertySymbols&&(r=r.concat(Object.getOwnPropertySymbols(t).filter(function(e){return Object.getOwnPropertyDescriptor(t,e).enumerable}))),r.forEach(function(n){var r;r=t[n],n in e?Object.defineProperty(e,n,{value:r,enumerable:!0,configurable:!0,writable:!0}):e[n]=r})}return e}function y(e,n){return n=null!=n?n:{},Object.getOwnPropertyDescriptors?Object.defineProperties(e,Object.getOwnPropertyDescriptors(n)):(function(e,n){var t=Object.keys(e);if(Object.getOwnPropertySymbols){var r=Object.getOwnPropertySymbols(e);t.push.apply(t,r)}return t})(Object(n)).forEach(function(t){Object.defineProperty(e,t,Object.getOwnPropertyDescriptor(n,t))}),e}function w(e){let{className:n,block:t,selectedValue:a,selectValue:s,tabValues:l}=e,d=[],{blockElementScrollPositionUntilNextRender:c}=(0,o.a_)(),u=e=>{let n=e.currentTarget,t=l[d.indexOf(n)].value;t!==a&&(c(n),s(t))},h=e=>{var n,t;let r=null;switch(e.key){case"Enter":u(e);break;case"ArrowRight":{let t=d.indexOf(e.currentTarget)+1;r=null!=(n=d[t])?n:d[0];break}case"ArrowLeft":{let n=d.indexOf(e.currentTarget)-1;r=null!=(t=d[n])?t:d[d.length-1]}}null==r||r.focus()};return(0,r.jsx)("ul",{role:"tablist","aria-orientation":"horizontal",className:(0,i.A)("tabs",{"tabs--block":t},n),children:l.map(e=>{let{value:n,label:t,attributes:s}=e;return(0,r.jsx)("li",y(f({role:"tab",tabIndex:a===n?0:-1,"aria-selected":a===n,ref:e=>{d.push(e)},onKeyDown:h,onClick:u},s),{className:(0,i.A)("tabs__item","tabItem_LNqP",null==s?void 0:s.className,{"tabs__item--active":a===n}),children:null!=t?t:n}),n)})})}function x(e){let{lazy:n,children:t,selectedValue:s}=e,o=(Array.isArray(t)?t:[t]).filter(Boolean);if(n){let e=o.find(e=>e.props.value===s);return e?(0,a.cloneElement)(e,{className:(0,i.A)("margin-top--md",e.props.className)}):null}return(0,r.jsx)("div",{className:"margin-top--md",children:o.map((e,n)=>(0,a.cloneElement)(e,{key:n,hidden:e.props.value!==s}))})}function b(e){let n=function(e){let n,{defaultValue:t,queryString:r=!1,groupId:i}=e,s=function(e){let{values:n,children:t}=e;return(0,a.useMemo)(()=>{let e=null!=n?n:p(t).map(e=>{let{props:{value:n,label:t,attributes:r,default:a}}=e;return{value:n,label:t,attributes:r,default:a}}),r=(0,u.XI)(e,(e,n)=>e.value===n.value);if(r.length>0)throw Error('Docusaurus error: Duplicate values "'.concat(r.map(e=>e.value).join(", "),'" found in <Tabs>. Every value needs to be unique.'));return e},[n,t])}(e),[o,g]=(0,a.useState)(()=>(function(e){var n;let{defaultValue:t,tabValues:r}=e;if(0===r.length)throw Error("Docusaurus error: the <Tabs> component requires at least one <TabItem> children component");if(t){if(!m({value:t,tabValues:r}))throw Error('Docusaurus error: The <Tabs> has a defaultValue "'.concat(t,'" but none of its children has the corresponding value. Available values are: ').concat(r.map(e=>e.value).join(", "),". If you intend to show no default tab, use defaultValue={null} instead."));return t}let a=null!=(n=r.find(e=>e.default))?n:r[0];if(!a)throw Error("Unexpected error: 0 tabValues");return a.value})({defaultValue:t,tabValues:s})),[f,y]=function(e){let{queryString:n=!1,groupId:t}=e,r=(0,l.W6)(),i=function(e){let{queryString:n=!1,groupId:t}=e;if("string"==typeof n)return n;if(!1===n)return null;if(!0===n&&!t)throw Error('Docusaurus error: The <Tabs> component groupId prop is required if queryString=true, because this value is used as the search param name. You can also provide an explicit value such as queryString="my-search-param".');return null!=t?t:null}({queryString:n,groupId:t});return[(0,c.aZ)(i),(0,a.useCallback)(e=>{var n,t;if(!i)return;let a=new URLSearchParams(r.location.search);a.set(i,e),r.replace((n=function(e){for(var n=1;n<arguments.length;n++){var t=null!=arguments[n]?arguments[n]:{},r=Object.keys(t);"function"==typeof Object.getOwnPropertySymbols&&(r=r.concat(Object.getOwnPropertySymbols(t).filter(function(e){return Object.getOwnPropertyDescriptor(t,e).enumerable}))),r.forEach(function(n){var r;r=t[n],n in e?Object.defineProperty(e,n,{value:r,enumerable:!0,configurable:!0,writable:!0}):e[n]=r})}return e}({},r.location),t=t={search:a.toString()},Object.getOwnPropertyDescriptors?Object.defineProperties(n,Object.getOwnPropertyDescriptors(t)):(function(e,n){var t=Object.keys(e);if(Object.getOwnPropertySymbols){var r=Object.getOwnPropertySymbols(e);t.push.apply(t,r)}return t})(Object(t)).forEach(function(e){Object.defineProperty(n,e,Object.getOwnPropertyDescriptor(t,e))}),n))},[i,r])]}({queryString:r,groupId:i}),[w,x]=function(e){let{groupId:n}=e,t=n?"docusaurus.tab.".concat(n):null,[r,i]=(0,h.Dv)(t);return[r,(0,a.useCallback)(e=>{t&&i.set(e)},[t,i])]}({groupId:i}),b=m({value:n=null!=f?f:w,tabValues:s})?n:null;return(0,d.A)(()=>{b&&g(b)},[b]),{selectedValue:o,selectValue:(0,a.useCallback)(e=>{if(!m({value:e,tabValues:s}))throw Error("Can't select invalid tab value=".concat(e));g(e),y(e),x(e)},[y,x,s]),tabValues:s}}(e);return(0,r.jsxs)("div",{className:(0,i.A)(s.G.tabs.container,"tabs-container","tabList__CuJ"),children:[(0,r.jsx)(w,f({},n,e)),(0,r.jsx)(x,f({},n,e))]})}function j(e){let n=(0,g.A)();return(0,r.jsx)(b,y(f({},e),{children:p(e.children)}),String(n))}},84429:function(e,n,t){t.d(n,{R:()=>s,x:()=>o});var r=t(96540);let a={},i=r.createContext(a);function s(e){let n=r.useContext(i);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:s(e.components),r.createElement(i.Provider,{value:n},e.children)}}}]);